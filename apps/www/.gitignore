# Next.js static-export output (regenerated by `next build` with output: 'export')
/out/

# Next.js generated ambient types (regenerated by `next build`/`next dev`)
next-env.d.ts

# Next.js dev/build cache (also covered by the root .gitignore, kept here for clarity)
/.next/
