{
  "_comment": "Isolated compatibility fixture for the historical Codex 0.131 hook adapter. Bypass-mode AIO and BoxLite images do not copy or register this file, and production task setup removes any inherited ~/.codex/hooks.json. The commands below are exercised only by explicit adapter verification or a future separately designed non-bypass integration.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "node /opt/cap/dist/hooks/permission-request.hook.js"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "node /opt/cap/dist/hooks/post-tool-use.hook.js"
          }
        ]
      }
    ]
  }
}
