# @cap/sandbox-workspace-git

Git-backed workspace bridge helpers.

This package owns the provider-neutral command construction and response parsing
used by adapters that materialize a task repository into a sandbox and deliver
changes back to a branch.
