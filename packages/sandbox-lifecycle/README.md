# @cap/sandbox-lifecycle

Provider-neutral lifecycle plans for sandbox task settlement.

The package has no provider dependency. It centralizes the order and flags for
capturing transcript, delivering workspace changes, stopping retained sandboxes,
tearing down terminal sessions, and releasing run slots.
