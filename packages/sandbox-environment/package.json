{
  "name": "@cap-console/sandbox-environment",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Provider-neutral sandbox environment source, validation, and resolution domain helpers for CAP.",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "pnpm --filter @cap-console/sandbox-core build && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "pnpm -w exec turbo run build --filter=@cap-console/sandbox-environment",
    "test": "node --test --test-force-exit \"test/**/*.test.mjs\"",
    "coverage": "c8 --100 --include=dist/**/*.js --reporter=text node --test --test-force-exit \"test/**/*.test.mjs\"",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@cap-console/sandbox-core": "workspace:*"
  },
  "devDependencies": {
    "@cap-console/eslint-config": "workspace:*",
    "@cap-console/tsconfig": "workspace:*",
    "@types/node": "^20.14.0",
    "eslint": "^9.27.0",
    "typescript": "^5.5.4"
  }
}
