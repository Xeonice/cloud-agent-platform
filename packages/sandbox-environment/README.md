# @cap/sandbox-environment

Provider-neutral sandbox environment domain helpers for CAP.

This package owns source descriptors, compatibility checks, validation result
types, and resolved non-secret metadata for sandbox runtime environments. It
does not talk to Prisma, Nest, Docker, BoxLite, or UI code directly.
