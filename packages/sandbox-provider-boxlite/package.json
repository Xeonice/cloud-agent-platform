{
  "name": "@cap-console/sandbox-provider-boxlite",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "BoxLite sandbox provider client and adapter for CAP.",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "pnpm --filter @cap-console/sandbox-core build && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "pnpm -w exec turbo run build --filter=@cap-console/sandbox-provider-boxlite --filter=@cap-console/sandbox-conformance",
    "test": "pnpm test:bridge && node --test --test-force-exit \"test/**/*.test.mjs\"",
    "test:bridge": "python3 ../../scripts/terminal-pty-byte-bridge.test.py",
    "test:e2e": "pnpm build && node --test --test-force-exit e2e/*.e2e.mjs",
    "coverage": "c8 --100 --include=dist/**/*.js --reporter=text node --test --test-force-exit \"test/**/*.test.mjs\"",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@cap-console/sandbox-core": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@cap-console/eslint-config": "workspace:*",
    "@cap-console/sandbox-conformance": "workspace:*",
    "@cap-console/tsconfig": "workspace:*",
    "@types/node": "^20.14.0",
    "@types/ws": "^8.5.12",
    "eslint": "^9.27.0",
    "typescript": "^5.5.4"
  }
}
