{
  "name": "@cap/tsconfig",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript base + per-target (node/library/next) tsconfig presets. Base enables strict: true.",
  "files": [
    "base.json",
    "node.json",
    "library.json",
    "next.json"
  ]
}
