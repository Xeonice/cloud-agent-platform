<!-- Track-annotated tasks. Each numbered group is a parallel Track:
     `## N. Track: <kebab-name> (depends: <track>|none)`.
     Tasks within a track run serially; independent tracks run in parallel at apply time.
     Backend tracks are contracts-first so frontend page tracks can consume real data. -->

## 1. Track: be-contracts (depends: none)

- [x] 1.1 Add `packages/contracts/src/session.ts`: zod schemas for `SessionUser` (githubId numeric, login, name, avatarUrl, allowed) and `AuthSession` (current-user-or-null), plus the `/auth/session` response shape; export from `index.ts`.
- [x] 1.2 Add `packages/contracts/src/github-import.ts`: `AvailableGithubRepo` schema (numeric `id`, `full_name`, `name`, `defaultBranch`, visibility, optional `description`) and `ImportRepoRequest` (selected GitHub repo identity); export from `index.ts`.
- [x] 1.3 Extend `packages/contracts/src/task.ts`: add OPTIONAL `branch` and `strategy` to the `Task`/`TaskResponse` schema and confirm `CreateTaskRequest` already declares matching `branch`/`strategy` shapes (sent value == readable value); keep `TaskStatus` enum including the distinct `agent_failed_to_start` value.
- [x] 1.4 Extend the `Repo` schema in contracts: add nullable GitHub-import metadata `description`, `defaultBranch`, `branchCount`, `updatedAt`, and an originating GitHub identity field for de-dup; ensure plain `gitSource`-only repos remain valid (fields nullable/absent).
- [x] 1.5 Add `packages/contracts/src/metrics.ts`: `MetricsResponse` composing a derived capacity block (`ceiling`, `active`, `free`, `queueDepth`, slot occupancy table `{slot, busy, taskId|null}`, queued ids FIFO, runner-minutes nullable) and a sampled resource block (per-container + aggregate CPU%/memory with `sampledAt`/age and an `available|stale|unavailable` flag); export from `index.ts`.
- [x] 1.6 Add `packages/contracts/src/audit.ts`: `AuditEvent` schema (id, taskId, attributed userId, `type`, `level` in `{info,warning,error}`, title, description, UTC `timestamp`, optional integer `resultCode`, session linkage runId) and `AuditQuery` (filter by `level`/all, by task `status`, `limit`); export from `index.ts`.
- [x] 1.7 Add `packages/contracts/src/settings.ts`: `AccountSettings` (read-only `allowedAccount` display identity, nullable `defaultRepoId`, `retention` days, `writeConfirm` toggle) and `CodexCredential` read shape (active `mode` official|compatible, state `not_connected|not_saved|connected`, optional base URL, `hasApiKey` boolean + masked suffix, selected default model) — never a plaintext key field; plus `UpdateSettingsRequest` and `SaveCodexCredentialRequest`; export from `index.ts`.
- [x] 1.8 Run `pnpm --filter @cap/contracts build` / `turbo typecheck` and confirm all new/extended schemas compile and are re-exported from `index.ts` with no breakage to existing consumers.

## 2. Track: be-oauth-allowlist (depends: be-contracts)

- [x] 2.1 Prisma migration in `apps/api/prisma/schema.prisma`: add `User` model (`id`, `githubId` unique numeric, `login`, `name`, `avatarUrl`, `allowed`, `createdAt`) and `Session` model (`id`, `userId` FK, opaque token hash, `expiresAt`, `createdAt`); generate the migration and `prisma generate`.
- [x] 2.2 In `apps/api/src/auth`, add the GitHub OAuth authorization-code flow: `GET /auth/github/login` (generate anti-CSRF `state`, persist server-side/signed cookie, redirect to GitHub authorize URL with `client_id`/scopes/`redirect_uri`/`state`) — refuse to start the flow when `GITHUB_CLIENT_ID`/`GITHUB_CLIENT_SECRET` are unset (fail-closed).
- [x] 2.3 Implement `GET /auth/github/callback`: verify `state` matches (reject mismatched/missing without exchanging code), exchange `code`→GitHub access token server-side using `client_secret` (never expose secret or raw token to browser), fetch GitHub numeric `id` + `login`; store the operator's GitHub access token server-side for later GitHub-import calls.
- [x] 2.4 Implement the hard allowlist gate at the single session-mint point: parse `AUTH_ALLOWLIST` keyed on immutable numeric GitHub `id` (login for display only); admit only listed ids, deny fail-closed when unset/empty/unparseable, reject non-allowlisted identity with no session and return to login gate (security denial, not a recoverable form error).
- [x] 2.5 On allowlisted login upsert the `User` record keyed by numeric `id` (create first login, refresh mutable `login`/avatar after); never let record persistence bypass the gate. Then establish a revocable server-side `Session` with expiry, set an opaque httpOnly+Secure+SameSite=Lax session cookie, and expose `GET /auth/session` (current user or 401) and `POST /auth/logout` (invalidate server-side + clear cookie).
- [x] 2.6 Add a session-validation guard and apply it to all protected REST endpoints (exempting `/health` and the OAuth start/callback): resolve cookie OR `bearer.<token>` subprotocol-carried token to a non-expired session, RE-CONFIRM allowlist membership at request time (de-allowlisted user denied on next request), reject missing/malformed/expired/revoked/non-allowlisted with 401 and no state change.
- [x] 2.7 Update the WebSocket handshake guard in `apps/api/src/terminal` to authenticate the session at connect time from the connect query param and/or `bearer.<token>` subprotocol (no `Authorization` header), and close unauthenticated/expired/revoked/non-allowlisted connections before joining any task stream or emitting bytes/control frames.
- [x] 2.8 Migrate the legacy operator token to a dual-accept path gated by `AUTH_TOKEN_LEGACY_ENABLED` (default `false`): accept `Authorization: Bearer <AUTH_TOKEN>` only when enabled, keep constant-time comparison on that path, keep the runner `TASK_TOKEN` trust domain fully distinct (a `TASK_TOKEN` SHALL NEVER authenticate an operator), and reject the operator bearer when the legacy path is disabled.
- [x] 2.9 Verify: state-mismatch rejection, refuse-to-start without OAuth creds, allowlisted admit / non-allowlisted fail-closed / empty-allowlist denies-all / numeric-id-not-login matching, logout invalidates server-side, expired session rejected, REST 401 paths, WS close-before-subscribe, legacy-token on/off — via api e2e/spec tests; `turbo typecheck`/build green.

## 3. Track: be-task-repo-persistence (depends: be-contracts)

- [x] 3.1 Prisma migration: add nullable `branch` and `strategy` columns to the `Task` model and nullable `description`, `default_branch`, `branch_count`, `updated_at` (+ originating GitHub identity column) to the `Repo` model in `apps/api/prisma/schema.prisma`; generate migration and `prisma generate`; confirm the migration provisions the columns against an empty DB.
- [x] 3.2 In `apps/api/src/tasks`, persist `branch`/`strategy` from the create-task body onto the `Task` record (no longer silently dropped) without altering clone/provision behavior (runner still checks out `branch` when supplied).
- [x] 3.3 Read `branch`/`strategy` back on EVERY task read path — create response (201), list-tasks, and fetch-by-id — echoing the supplied value or null when omitted (never stale/fabricated); confirm the lifecycle (status enum + transitions, including `agent_failed_to_start`) is unaffected by these inert run parameters.
- [x] 3.4 In `apps/api/src/repos`, surface the GitHub-import metadata fields on repo list and fetch-by-id read responses when present, returning null/absent for plain-gitSource repos; keep create-body validation (400 on invalid) unchanged.
- [x] 3.5 Verify: write/read round-trip of `branch`/`strategy` in Postgres, omitted→null read-back, list/fetch/create agreement, repo-metadata-when-present vs null, lifecycle transitions unchanged with branch/strategy set — via tasks/repos spec tests; `turbo typecheck`/build green.

## 4. Track: be-github-import (depends: be-oauth-allowlist, be-task-repo-persistence)

- [x] 4.1 Add a `github-import` module under `apps/api/src` (or extend `repos`): `GET` available-GitHub-repos endpoint that calls GitHub `GET /user/repos` server-side using the REQUESTING operator's own stored OAuth access token (per-operator, never a shared token, token never returned to browser), session-gated (401 on missing/invalid), returning each repo's numeric `id`, `full_name`, default branch, visibility, description.
- [x] 4.2 Surface GitHub listing errors distinguishably: a missing/expired/revoked GitHub token → a "GitHub authorization required" signal (not session-401, not empty list); rate-limit/outage/network → a cause-preserving (429/5xx-class) retry-able error; never conflate empty-but-successful with failure.
- [x] 4.3 Add the import endpoint (extending the `Repo` create path): create a platform `Repo` from a selected GitHub repo with `name`, derived `gitSource`, and GitHub-import metadata (originating numeric `id`/`full_name`, default branch, description); session-gated; return 201 with the created `Repo` and its generated platform id.
- [x] 4.4 De-duplicate imports keyed on the originating GitHub stable identity (numeric `id`, `full_name` fallback) — not the mutable display name: on re-import respond 409 identifying the existing `Repo` (or idempotently return it); make the available list reconcilable against imported `Repo`s so the console can mark already-imported entries.
- [x] 4.5 Add set-default-repo persistence + read-back: designate exactly one imported platform `Repo` as default (new default clears prior, at most one), reject defaulting to an un-imported available repo, and expose a read of the current default; keep imported-repos the only source of truth for task-creation repo selection.
- [x] 4.6 Verify: per-operator token isolation, 401 unauthenticated, re-auth vs empty vs retry-able error states, import creates Repo with provenance, 409 de-dup keyed on GitHub id (distinct same-name repos allowed), set/clear/read default, only-imported-selectable — via spec tests; `turbo typecheck`/build green.

## 5. Track: be-metrics (depends: be-contracts, be-oauth-allowlist)

- [x] 5.1 In `apps/api/src/guardrails` (semaphore.ts/guardrails.service.ts), expose exact point-in-time projections of `ConcurrencySemaphore` state: `ceiling` (maxConcurrentTasks), `active` (runningCount), `free` (ceiling−active, never negative, active+free==ceiling), `queueDepth` (queuedCount) — read live at request time, no parallel/drifting counter.
- [x] 5.2 Build the slot occupancy table from `snapshotRunning()`/`snapshotQueue()`: exactly `ceiling` slots each `busy`(taskId)|`idle`, plus queued ids in FIFO order separate from free slots; busy count==active, idle count==free, queued length==queueDepth; never invent slots beyond the ceiling.
- [x] 5.3 Add a background CPU/memory sampler (new file under `apps/api/src/guardrails` or a `metrics` module): sample running `cap-aio-<taskId>` containers via cgroup v2 reads (`cpu.stat`, `memory.current`/`memory.max`) preferred or `docker stats --no-stream` fallback, on a bounded configurable ~5s cadence, caching the last snapshot tagged `sampledAt`; CPU% from delta of two readings, memory against cgroup limit; skip sampling when zero containers run and report explicit empty/zero "no active containers" (not a stale prior sample).
- [x] 5.4 Derive runner-minutes accounting from task running durations (admission→terminal timestamp, in-flight counted to now) labeled as derived accounting; report `unavailable` (not fabricated/zero-as-exact) when timing data is insufficient.
- [x] 5.5 Add the session-gated `GET /metrics` aggregation endpoint composing the exact derived capacity block + the cached sampled resource block in one round trip, each labeled derived(exact/live) vs sampled(with timestamp/age); a sampling outage/staleness degrades ONLY the sampled block (still return derived capacity, mark sampled unavailable/stale); reject unauthenticated/non-allowlisted with 401 before serializing any task ids/queue depth/CPU-memory.
- [x] 5.6 Verify: live active/free/queue from semaphore, immediate change on slot take/free, no-drift, slot table↔scalar consistency, cadence-bounded sampling decoupled from request rate, memory-vs-limit, no-containers empty reading, single-round-trip composition, sampling-outage degrades only sampled block, 401 leaks nothing — via guardrails/metrics tests; `turbo typecheck`/build green.

## 6. Track: be-audit-approvals (depends: be-contracts, be-oauth-allowlist, be-task-repo-persistence)

- [x] 6.1 Prisma migration: add an append-only `AuditEvent` table (id, taskId FK, attributed userId, `type`, `level`, title, description, UTC `timestamp`, optional `resultCode`, runId linkage); generate migration + `prisma generate`; events never mutated/deleted by lifecycle progression and never orphaned (every event references a real task id).
- [x] 6.2 Emit audit events from `apps/api/src/guardrails` at each lifecycle transition (`task.created`, `running`, `completed`, `failed`, `cancelled`, `agent_failed_to_start`, and force-fail causes deadline/idle/circuit-break) attributing to the GitHub-identity user; recording is best-effort (a persistence failure logs but does NOT roll back or block the transition).
- [x] 6.3 Assign HTTP-status-like `resultCode`s consistent with `level`: creation `201`/info, read-or-transition-no-new-resource `200`, conflict/admission-slot conflict `409`, validation/precondition `422`; enforce that a `2xx` never pairs with `error` and a `4xx`/`5xx` never pairs with `info` so the audit-dot color and the row status code never contradict.
- [x] 6.4 Add the session-gated audit query endpoint returning recent events most-recent-first, filterable by severity `level` (info/warning/error + all) and by task `status`, honoring a default cap + caller `limit`; each event carries `type`/`level`/`timestamp`/title/description/`resultCode`/taskId/session-linkage for `/tasks/$taskId` deep-link; 401 (no read) on missing/expired/non-allowlisted session; support querying a single task's full ordered sequence by task id (including after terminal state).
- [x] 6.5 Add the session-gated approvals pending-list read endpoint surfacing pending `PermissionRequest` decisions (reusing `packages/contracts/src/approvals.ts`).
- [x] 6.6 Verify: lifecycle transition records an event with cause, force-fail→error with distinguishing type, append-only never-rewritten ordered sequence, persistence-failure does not block transition, resultCode↔level never contradict, most-recent-first + level/status filter + limit honored, 401 unauthenticated, no orphaned events, approvals pending list — via audit/approvals tests; `turbo typecheck`/build green.

## 7. Track: be-account-settings (depends: be-contracts, be-oauth-allowlist, be-github-import)

- [x] 7.1 Prisma migration: add an `AccountSettings` table (per-user: read-only allowedAccount sourced from OAuth identity, nullable defaultRepoId, retention days, writeConfirm) and a `CodexCredential` table (per-user: mode official|compatible, connection state, optional base URL, ENCRYPTED api key ciphertext, selected default model); generate migration + `prisma generate`.
- [x] 7.2 In `apps/api/src/creds` (or a `settings` module), add the session-gated settings read API returning the current allowlisted account's preferences (allowedAccount display identity, defaultRepoId or null, retention, writeConfirm), per-account scoped (never leaks across accounts), returning defaults when nothing saved.
- [x] 7.3 Add the settings update API (PATCH/PUT) validating against the contracts schema and returning the updated sanitized settings: accept a defaultRepo only when it references a repo the account has imported (else 4xx, unchanged), constrain retention to the allowed set/range, reject invalid bodies with 400 and mutate nothing; reject/ignore attempts to write the read-only allowedAccount display identity (governed by the allowlist, not editable).
- [x] 7.4 Implement Codex credential storage as a concept distinct from console login: two mutually-exclusive modes (official = connection state + non-secret metadata; compatible = base URL + API key + optional default model); read API returns active mode + state `not_connected|not_saved|connected` so status card / tab subtitle / provider pill render consistently; connecting/clearing a Codex credential never alters OAuth identity/allowlist and console logout never erases the credential.
- [x] 7.5 Encrypt the compatible-provider API key at rest (AES-GCM with a configured server key) and NEVER return plaintext: reads expose only `hasApiKey` and/or a masked suffix; plaintext exists in memory only transiently for save/decrypt-at-execution and is never logged; updating other fields without re-sending the key preserves the stored encrypted key (not overwritten with empty).
- [x] 7.6 Add compatible-provider model discovery (using base URL + key, available WITHOUT first persisting so a candidate can be validated) returning selectable model ids and allowing one to be saved as default; a failed discovery returns a clear error and does NOT mark the credential `connected`. Add a test/validate endpoint reporting success or descriptive failure without exposing the key and not flipping state to `connected` on failure.
- [x] 7.7 Verify: read returns saved-or-default scoped per account, allowedAccount read-only, valid update reads back, default-must-be-imported 4xx, invalid body 400 leaves state, two-mode storage + not_saved state, key encrypted-at-rest + never echoed + preserved on partial update, model discovery + persist default + failed-discovery-no-connect, credential test success/failure-no-flip — via settings/creds tests; `turbo typecheck`/build green.

## 8. Track: fe-scaffold (depends: be-contracts)

- [x] 8.1 Remove Next artifacts in `apps/web`: delete `next.config.mjs`, `next-env.d.ts`, the Next-shaped `vercel.json`; in `package.json` drop `next`, add exactly-pinned `@tanstack/react-start`, `@tanstack/react-router`, `@tanstack/react-query`, `@tanstack/react-router-ssr-query`, `@tanstack/react-router-devtools`, `vite`, `@vitejs/plugin-react`, `@tailwindcss/vite`, `tailwindcss@4`, `tw-animate-css`, `vite-tsconfig-paths`, `react@19`/`react-dom@19`/`@types/react@19`; keep `@cap/ui`/`@cap/contracts` as `workspace:*`.
- [x] 8.2 Write `apps/web/vite.config.ts` with the load-bearing plugin order `tailwindcss()` → `tanstackStart({ srcDirectory: 'src' })` → `viteReact()` → `nitro()`, plus `tsconfigPaths()`.
- [x] 8.3 Update `apps/web/tsconfig.json`: extend `@cap/tsconfig/base.json` (drop the Next plugin), set `paths {"@/*":["./src/*"]}`, `moduleResolution: Bundler`.
- [x] 8.4 Create `src/router.tsx` (`getRouter()` factory: NEW `QueryClient` per request + `createRouter({ context:{ queryClient }})` + `setupRouterSsrQueryIntegration` — never a module singleton), `src/start.ts` (`createStart(() => ({ defaultSsr: true }))`), and `src/routes/__root.tsx` (`createRootRouteWithContext`, `<HeadContent>`/`<Outlet>`/`<Scripts>`, inject `app.css?url` into head links, Sonner `<Toaster>` placeholder, inline theme pre-hydration script setting `.dark` before paint).
- [x] 8.5 Create the 10 route placeholder files (`index`, `login`, `workspace`, `resume`, `_app`, `_app/dashboard`, `_app/repositories`, `_app/history`, `_app/settings`, `_app/tasks/new`, `_app/tasks/$taskId`), each rendering only its page name; set `/tasks/$taskId` `ssr:false` + a `pendingComponent` placeholder.
- [x] 8.6 Migrate reused lib: copy `ws-client.ts` (`TerminalSocket`, keep token query + `bearer.<token>` subprotocol), `api-client.ts`→`lib/api/real.ts`, `config.ts`; change env reads `NEXT_PUBLIC_*`→`import.meta.env.VITE_*`; add `.env.example` (`VITE_API_BASE_URL`/`VITE_WS_URL`/`VITE_AUTH_TOKEN`).
- [x] 8.7 Verify: `pnpm --filter @cap/web dev` boots, each placeholder route is reachable, `routeTree.gen.ts` generates (gitignored), `pnpm --filter @cap/web build` (Vite build) passes with NO `next` dependency, `turbo typecheck` green.

## 9. Track: fe-tokens-shadcn (depends: fe-scaffold)

- [x] 9.1 Write `src/styles/app.css` header (`@import "tailwindcss"; @import "tw-animate-css"; @custom-variant dark (&:is(.dark *))`) and `:root` mapping the `admin-*` light final values onto shadcn semantic tokens (note buttons are dark: `--primary:#171717`/`--primary-foreground:#fff`; `--background:#fff`/`--foreground:#171717`; accent blue `#0a72ef`), the full `--sidebar-*` set (active = dark pill), and `--chart-1..5`.
- [x] 9.2 Add the extra brand/semantic tokens to `:root`: `--success:#1a7f37`/`--success-soft:#ecfdf3`, `--warning`/`-soft`, `--info`/`-soft`, `--danger-soft`, `--dark-pill:#171717`, brand originals, and terminal scope `--terminal-bg:#050505`/`--terminal-fg:#e8e8e8`/`--terminal-muted`/`--terminal-line` + oklch ok/warn/err log colors.
- [x] 9.3 Write the `@theme inline` block mapping every `--xxx`→`--color-*` utility (incl. `bg-success-soft text-success`, `bg-terminal-bg`), sans/mono font stacks (mono + `tabular-nums`), the radius ladder (sm 6 / DEFAULT 8 / lg 10 / xl 12 / full), and the box-shadow tokens (`shadow-ring/card/modal/menu/toast/terminal` mirroring the prototype's 1px-ring "borders").
- [x] 9.4 Synthesize the `.dark` block (chrome background/card/foreground/border inverted, status/accent hues preserved & lightened) for the terminal scope and future use.
- [x] 9.5 `shadcn init` (`pnpm dlx shadcn@latest init -t start -c apps/web`); set `components.json` `tailwind.config:""`, `css:"src/styles/app.css"`, `cssVariables:true`, `baseColor:"neutral"`, `rsc:false`, `tsx:true`.
- [x] 9.6 Install all blueprint shadcn components into `apps/web` (`-c apps/web`): button card badge dialog drawer input textarea select checkbox label form tabs toggle-group dropdown-menu avatar progress sonner skeleton separator navigation-menu breadcrumb sidebar table tooltip scroll-area.
- [x] 9.7 Migrate `packages/ui/src/styles.css` off the legacy v3 three-directive + HSL form to consume the SAME v4 token contract as `app.css` (or export components-only with tokens centralized in the app) so shared `Button`/`Card`/`Badge`/`Terminal` never desaturate ("脱色"); confirm the `<Terminal>` still loads fit/serialize/unicode11 addons.
- [x] 9.8 Verify: build a temporary `/styleguide` page rendering all installed components + StatusPill 6 variants + dark/secondary/ghost buttons + shadow cards, cross-check color values against the token table; `turbo typecheck`/build green.

## 10. Track: fe-data-layer (depends: fe-scaffold)

- [x] 10.1 `src/lib/api/capabilities.ts`: `BACKEND_CAPABILITIES` flag map (`tasks`/`repos`/`createTask: true`; `auth`/`metrics`/`history`/`settings`/`githubImport`/`branches: false`), with a comment per flag noting the absent-endpoint reason it gates.
- [x] 10.2 `src/lib/api/real.ts`: confirm `listTasks`/`getTask`/`listRepos`/`createTask` parse against `@cap/contracts` schemas (carried from `api-client.ts`), keep `ApiError` 401/404 branches; stub the real functions for the not-yet-flipped domains (auth/metrics/history/settings/githubImport) so flipping a flag only requires implementing one function.
- [x] 10.3 `src/lib/api/mock.ts`: typed mocks `mockAuth` (session + allowlist `tanghehui`), `mockMetrics` (RUNNERS 7/10, QUEUE 11, CPU 42/内存 64, 活跃/待处理/Runner分钟/空闲槽位, 10-slot states), `mockHistory` (audit event stream + result codes + 耗时), `mockSettings` (allowedAccount/retention/writeConfirm/Codex state), `mockGithubImport` (USER_REPOSITORIES 4 repos + metadata), `mockTaskContexts` (taskId→repo#branch/agent/运行环境/安全边界); base on `@cap/contracts` types extended with local view types for backend-absent fields; uniform `delay(120–420ms)`.
- [x] 10.4 `src/lib/api/queries.ts`: `queryOptions` factories (`tasksQuery` `refetchInterval:5000`, `taskQuery`, `reposQuery`, `metricsQuery`, `capacityQuery`, `historyEventsQuery`, `settingsQuery`, `authSessionQuery`, `githubReposQuery`, `taskContextQuery`), each `queryFn` choosing real/mock via `BACKEND_CAPABILITIES`; loaders and components share the same keys.
- [x] 10.5 Mutations: `createTaskMutation` (real `POST /repos/:repoId/tasks`, `onSuccess` invalidate `['tasks']`), `importRepoMutation`/`setDefaultRepoMutation`/`saveSettingsMutation` (write store + invalidate affected queries), reproducing the prototype's read-state/render loop.
- [x] 10.6 `src/lib/store.ts` (lightweight persisted store, key `agent-control-plane-state`: `githubConnected`/`importedRepos`/`selectedRepo`/`settings`/`codexCredential`, dedup imported set, unique default) and `src/lib/mock-session.ts` (`isAuthenticated`/`login`/`logout` token gate, reading real OAuth session when `auth` capability is on else the client gate).
- [x] 10.7 `src/hooks/use-client-filter.ts` (search + level/status segmented + live visible count, `useMemo`-derived, shared by history + dashboard) and `src/hooks/use-account-menu.ts` (Esc/outside-click close, `aria-expanded`).
- [x] 10.8 Verify: minimal vitest asserting capability flip switches real↔mock; each mock return validates against its contracts/view type; derived filters stay out of the cache; `turbo typecheck` green.

## 11. Track: fe-app-shell (depends: fe-tokens-shadcn, fe-data-layer)

- [x] 11.1 Build `src/routes/_app.tsx` pathless layout: `SidebarProvider` + `AppSidebar` (brand mark; nav items 任务控制台 / 仓库导入 / 历史日志 with ⌘1/⌘2/⌘3 mono hints + active dark pill) + `SidebarInset` + sticky blurred `Topbar` (breadcrumb eyebrow + right-side action slot).
- [x] 11.2 Build `AccountMenu` (Avatar `TH` initials + DropdownMenu 打开设置/退出登录 + OAuth-verified status dot; shared by desktop & mobile; 退出登录 calls `mock-session.logout()`) and `MobileNav` (fixed bottom bar 4 columns 控制台/仓库/历史/账户, hidden on desktop; 账户 opens the same `AccountMenu`).
- [x] 11.3 Implement `_app` `beforeLoad` auth gate: unauthenticated visitor to any app-shell route redirected to `/login` BEFORE app-shell content renders (read via the auth session source — real OAuth session when `auth` capability on, else client gate); sign-out clears session and navigates `/login`.
- [x] 11.4 Build the standalone `LandingNav` (brand AC + anchor/product links + single CTA `Button asChild Link`) for reuse by `/`, `/workspace`, `/resume`.
- [x] 11.5 Implement nav active highlighting: map `/tasks/$taskId` (session) and `/tasks/new` (create) back to the 任务控制台 (dashboard) item using `useRouterState`/`matchRoute`.
- [x] 11.6 Verify: app pages show sidebar+topbar+bottom-bar, AccountMenu open/Esc/outside-click + `aria-expanded`, unauthenticated→`/login`, mobile breakpoint reveals bottom nav, session/create routes highlight dashboard; `turbo typecheck`/build green.

## 12. Track: fe-page-landing-login (depends: fe-app-shell, be-oauth-allowlist)

- [x] 12.1 `/` Landing in `src/routes/index.tsx`: `LandingNav` + Hero (eyebrow/title/dual CTA/trust pills/3 `ProofTile`s) + `HeroPreview` (macOS window-bar traffic dots + mini task rows + stat tiles + static terminal) + `#workflow` 3-step + `#security` 3-card; smooth anchor scroll (scroll-margin offsetting fixed nav); fully static, CTAs→`/login`/`/dashboard`; SSR-safe (no `Date.now()`/`Math.random()` rendered directly).
- [x] 12.2 `/login` in `src/routes/login.tsx`: dual-column auth card (left brand + copy + GitHub 授权 button with mutually-exclusive empty/success states; right 保障 sidebar 3 `InstallStep` active highlight + `ConfigList`); the authorize action triggers the auth/login flow, toasts success ("已完成授权"), routes into 仓库导入 `/repositories`; already-authenticated visitor MAY be redirected to `/workspace`.
- [x] 12.3 Wire `/login` to real GitHub OAuth when `auth` capability is on: the 授权 button initiates `GET /auth/github/login`; on allowlisted callback the session establishes and the gate admits; render the non-allowlisted rejection copy (e.g. "该 GitHub 账号不在白名单内，无法进入控制台") when the callback denies. Keep the mock token-gate path behind the capability flag.
- [x] 12.4 Reuse `StatusPill`/`ProofTile`/`CountChip`; keep all Chinese full-width punctuation copy verbatim.
- [x] 12.5 Verify: Landing SSR no hydration warning + working anchors, login empty↔success toggle + success toast + route to `/repositories`, OAuth-on path admits allowlisted / shows rejection copy for non-allowlisted; `turbo typecheck`/build green.

## 13. Track: fe-page-workspace-resume (depends: fe-app-shell)

- [x] 13.1 `/workspace` Launcher in `src/routes/workspace.tsx`: `LandingNav` + Hero + ops-strip 3 `StatTile` (REPOSITORIES from `reposQuery`; RUNNERS/QUEUE from `metricsQuery` mock) + 6 screen-cards (each a full-card `Link`, pill blue/dark/multi, footer "open tasks" count + latest run id derived from `tasksQuery`); nav/CTA route to dashboard/repositories/history/settings/session/landing.
- [x] 13.2 `/resume` Handoff in `src/routes/resume.tsx`: `LandingNav` + main panel (eyebrow/title/lead/dual CTA) + 3 `StatTile` (NEXT ACTION from the highest-priority waiting-input task, parameterizing the second CTA's `/tasks/$taskId` deep link; DEFAULT SCOPE from `selectedRepo`; SAFETY static); CTAs→`/dashboard`/`/tasks/$taskId`.
- [x] 13.3 Keep both pages SSR-safe (no nondeterministic value rendered directly) and Chinese copy verbatim.
- [x] 13.4 Verify: workspace counts reflect repos/tasks queries, resume next-action drives the CTA deep link, both pages hydrate without warnings; `turbo typecheck`/build green.

## 14. Track: fe-page-repositories-settings (depends: fe-app-shell, be-github-import, be-account-settings)

- [x] 14.1 `/repositories` in `src/routes/_app/repositories.tsx`: screen-header (添加仓库 button) + 4 stat-tiles (DEFAULT bound to `selectedRepo`) + imported-repos panel (Card list with column headers + imported-count Badge from `reposQuery`) + import Dialog with pending-empty→loading→filterable-candidate-list flow (candidates from `githubReposQuery`, imported from `reposQuery`); importing adds to `importedRepos`, sets default, toasts; `setAsDefault`; dedup/unique-default; Dialog a11y (`role`/`aria-modal`/`aria-labelledby`/Esc/backdrop/focus).
- [x] 14.2 Wire `/repositories` real path when `githubImport` capability on: candidate list reads the available-GitHub-repos endpoint and marks already-imported entries; import runs `importRepoMutation` (real import → 409 already-imported handling); set-default runs `setDefaultRepoMutation`; distinguish empty vs error vs re-authorization-required states from the backend error signals.
- [x] 14.3 `/settings` in `src/routes/_app/settings.tsx`: left secondary anchor nav (account/github/codex/safety) + system-strip 3 cards + settings-grid (identity card Avatar + access-and-defaults form: `allowedAccount` read-only, default repo from `reposQuery`, `retention`, `writeConfirm`) + Codex login section (status card + Tabs 官方 Codex / 兼容提供方).
- [x] 14.4 Codex two dialogs: direct authorize (scope list + connect/connected states) and api-key (Base URL + API Key password + 获取可用模型→model-picker→select default→save/test); credential state 未连接/未保存/已连接 stays synchronized across status card, tab subtitle, provider pill; a saved API key is never re-displayed in plaintext; keep GitHub OAuth (谁能进控制台) vs Codex credential (任务运行用什么模型) as distinct concepts.
- [x] 14.5 Saving runs `saveSettingsMutation` (write store + invalidate `settingsQuery`); reset restores defaults; wire to the real settings/Codex endpoints when the `settings` capability is on (default-must-be-imported validation, key encrypted/masked, model discovery, credential test).
- [x] 14.6 Verify: import flow empty→loading→filter→import→default + 409 dedup, default uniqueness, settings save/reset + default-must-be-imported, Codex tab switch + both dialogs + model discovery + masked saved key, mobile bottom-bar; `turbo typecheck`/build green.

## 15. Track: fe-page-history (depends: fe-app-shell, be-audit-approvals)

- [x] 15.1 `/history` in `src/routes/_app/history.tsx`: screen-header + history-summary 3 stat-tiles (ACTIVE WINDOW/ATTENTION derived from tasks, RETENTION from settings/metrics) + audit-toolbar (search Input + level `SegmentedControl` 全部/信息/警告/错误 + visible `CountChip`).
- [x] 15.2 Two-column grid: left 最近任务 Table (任务/仓库/结果/耗时/会话记录, result `StatusPill` green/warn/dark/default, session link→`/tasks/$taskId`) from `tasksQuery` (+ mock 耗时/rich result until backend supplies); right `AuditTimeline` (time + warn/danger dot + title/description + right-side HTTP status 200/201/409/422) from `historyEventsQuery`.
- [x] 15.3 A single client-side filter (`use-client-filter`: search + level) drives BOTH the left table rows and the right events simultaneously with the visible count updating live; page is read-only (no terminal/WS), SSR-friendly.
- [x] 15.4 Wire the real path when `history` capability on: `historyEventsQuery` reads the audit query endpoint (level/status filter + limit, most-recent-first); session linkage deep-links rows to `/tasks/$taskId`.
- [x] 15.5 Verify: one filter drives both columns, count updates with results, session link navigates, status-code/dot coloring correct, SSR hydrates without errors; `turbo typecheck`/build green.

## 16. Track: fe-page-dashboard (depends: fe-app-shell, be-metrics, be-task-repo-persistence)

- [x] 16.1 `/dashboard` in `src/routes/_app/dashboard.tsx`: workbench-header (time-range `SegmentedControl` 24h/7d/30d client window + 新建任务 button) + ops-status-bar 4 `MetricTile` (活跃/待处理/Runner分钟/空闲槽位 from `metricsQuery`); make `/dashboard` the post-login default landing surface.
- [x] 16.2 Left task queue panel: search Input (`data-task-search`) + status `SegmentedControl` (全部/等待输入/排队中) + `CountChip`; `QueueRow` list from `tasksQuery` (`real`), `data-status` driving status dot + row tint + progress bar + status pill, needs-input sorted to top, enter session→`/tasks/$taskId`, queued rows `aria-disabled` until connectable; task list polls 5s `refetchInterval` (+`refetchIntervalInBackground` if needed).
- [x] 16.3 Right Agent-capacity aside: 占用 7/10 + free-slot pills + `SlotMeter` (10-segment busy/warn/idle) + `ResourceMeter` (CPU 42/内存 64 warn) + `ConfigList` (scheduling region/handoff/write boundary) from `metricsQuery`/`capacityQuery`.
- [x] 16.4 New-task Dialog two columns: left form (repo Select from `reposQuery`, branch Select, strategy Select, 任务描述 Textarea + live word count, 破坏性写入前停止 Checkbox default checked) + right preflight (3 `ReviewStep` complete/warn + live `agentctl` `CommandPreview`); submit via `createTaskMutation`→surface run id + deep-link into `/tasks/$taskId`, persist `selectedRepo`/`branch`/`latestRunId`, invalidate tasks; render branch/strategy controls without misrepresenting unsent/unread fields as confirmed task state.
- [x] 16.5 Loader uses `ensureQueryData(tasksQuery + reposQuery)` to avoid waterfalls; preserve aria (pressed/modal/labelledby/disabled/haspopup); when `metrics` capability on the tiles/slot-meter/resource-meter read the single `/metrics` round trip (derived capacity + sampled CPU/mem, sampled-block-stale degradation); when `branches` capability on, branch/strategy read back on the task.
- [x] 16.6 Verify: queue render + search/status filter + needs-input top, Dialog open/focus/backdrop, command preview reacts to form state, word count, submit→run id + session deep-link, 5s polling refresh, capacity tiles from one `/metrics` response; `turbo typecheck`/build green.

## 17. Track: fe-page-create-task (depends: fe-page-dashboard)

- [x] 17.1 `/tasks/new` in `src/routes/_app/tasks/new.tsx`: reuse the dashboard form components + `CommandPreview`/`ReviewStep`; Preflight 3 stat-tiles (REPOSITORY from `reposQuery` / RUNNER mock / GUARDRAIL static) + left task-form card (repo/branch/strategy/description+word-count/破坏性写入 Checkbox) + right side-stack (command preview + execution-boundary `ConfigList`); keep `clientOnly:false` (command preview is styled mono text, not xterm).
- [x] 17.2 Submit via the SAME shared `createTaskMutation`: on success show a success result pill (已创建 task_xxxx · 正在 iad-02 排队 + link into `/tasks/$taskId`), persist `selectedRepo`/`branch`/`latestRunId` to store, toast ("任务已进入远端 Agent 队列").
- [x] 17.3 Topbar breadcrumb tanghehui/agent-control/tasks/new + 取消→`/dashboard`; command-preview credential hardcoded `codex-official`; repo options restricted to imported repos (security scope = imported set).
- [x] 17.4 Verify: live command preview + word count, submit success pill + deep-link, cancel returns, behavior identical to the dashboard modal; `turbo typecheck`/build green.

## 18. Track: fe-page-session (depends: fe-app-shell, be-task-repo-persistence)

- [x] 18.1 `/tasks/$taskId` in `src/routes/_app/tasks/$taskId.tsx`: route `ssr:false` + `pendingComponent` TerminalSkeleton (real, window-free, no flash); topbar (Breadcrumb last segment taskId + live-connection `StatusPill` + 返回任务/复制会话记录/暂停输出 buttons) + session header + session-context-strip 3 cards (任务目标/运行环境/安全边界 from `taskContextQuery` mock, taskId base fields from `taskQuery` real).
- [x] 18.2 `SessionTerminal`: mount `@cap/ui <Terminal>` in `useEffect`, connect `TerminalSocket(taskId)`; `onRaw`→`term.write` (raw bytes bypass Query), `onOpen`→`sendReconnect(lastSeq,cols,rows)`, `onResize`→`sendResize`; command input row Input + send/Enter→`sendKeystroke` (lease-constrained); ResizeObserver fit.
- [x] 18.3 Fallback: when `window.Terminal`/xterm unavailable, render a fallback DOM line view (terminal-line dim/ok/warn) + command input row instead of crashing; theme xterm by resolving `--terminal-*` to hex into the `theme` option (bg `#050505`/fg `#e8e8e8`/JetBrains Mono/fontSize mobile 12 else 13/lineHeight 1.45/cursorBlink); `import "@xterm/xterm/css/xterm.css"`.
- [x] 18.4 Control-frame bridge: in `onControl`, task-completion/lease/approval-decision→`queryClient.setQueryData(['tasks',id])` or invalidate; pending `PermissionRequest`→approval surface (allow/deny resolving independently of the write lock); pause/resume toggles `paused`+button copy+toast; 复制会话记录 reads term text→clipboard (degrade on failure); 返回→`/dashboard`.
- [x] 18.5 Verify: real WS streaming + command echo (api+sandbox up), pause/resume backpressure, reconnect snapshot/tail_replay, approval allow/deny, copy, fallback path; `ssr:false` server emits skeleton only (no `window` access); `turbo typecheck`/build green.

## 19. Track: integration-delivery (depends: fe-page-landing-login, fe-page-workspace-resume, fe-page-repositories-settings, fe-page-history, fe-page-dashboard, fe-page-create-task, fe-page-session, be-github-import, be-metrics, be-audit-approvals, be-account-settings)

- [x] 19.1 Delete the temporary `/styleguide` page; unify error/notFound/pending components (`errorComponent` handles 401→login-gate prompt).
- [x] 19.2 Global regression: all 10 routes reachable, app-shell vs standalone assignment correct, mobile breakpoint, global Sonner, theme pre-hydration with no FOUC, all Chinese full-width-punctuation copy verbatim.
- [x] 19.3 Configure the Nitro `vercel` preset in `vite.config.ts` (replacing the old Next `vercel.json`), verify `VITE_*` env injection and cross-origin CORS with `credentials:'include'` so Vercel web targets the Fly/compose api origin and the session cookie + WS subprotocol auth work cross-origin.
- [x] 19.4 Flip every `BACKEND_CAPABILITIES` flag to `true` against the running api (docker-compose), implement the matching `real.ts` functions, and verify each page's real data path end-to-end (auth/OAuth gate, GitHub import, `/metrics`, audit/history, settings/Codex, branch/strategy read-back); confirm the `single-user-auth`→`multi-user-oauth` migration (legacy `AUTH_TOKEN_LEGACY_ENABLED` dual-accept then off) and that `/health` stays unauthenticated and `TASK_TOKEN` stays distinct.
  - VERIFIED (2026-06-06, legacy-token e2e vs compose api): 6 migrations auto-applied; 401 fail-closed -> 200 gated auth; /metrics (real ceiling 5 + docker-stats sampling), /repos + /tasks (branch/strategy read-back), /audit/events real data paths; dashboard renders live backend capacity (0/5) via CORS + session. Fixed 4 latent real.ts path mismatches (/history,/github/repos,/github/import,/repos/default). /health stays unauthenticated.
  - VERIFIED FULL OAuth e2e (2026-06-06, GitHub OAuth App registered, user Xeonice/13403586): real GitHub OAuth login → allowlist gate admit → cross-origin session cookie (SameSite=None+Secure) → /auth/session + /settings (allowedAccount=Xeonice) + Codex state real data, on BOTH client navigation AND SSR first paint. Fixed 3 cross-origin production defects found in e2e: callback relative-redirect → WEB_ORIGIN absolute, SameSite=Lax → cross-origin-aware None+Secure, and SSR loader cookie forwarding (lib/server-cookie.ts). ALL BACKEND_CAPABILITIES flags now true (real). single-user-auth → multi-user-oauth migration confirmed; /health stays unauthenticated; legacy AUTH_TOKEN dual-accept path intact.
- [x] 19.5 Run `turbo verify` (typecheck + lint + build) fully green across `apps/web`, `apps/api`, `packages/contracts`, `packages/ui`; optionally Playwright per-page screenshot comparison of the 10 pages against the prototype baselines for high-fidelity acceptance.
- [x] 19.6 Update README/docs: web migrated to TanStack Start, the multi-user OAuth + allowlist security boundary over host-root docker.sock, the capabilities-flag real/mock seam and how flipping a flag points a page at the real backend.
