<!-- Track-annotated tasks. Each numbered group is a parallel Track:
     `## N. Track: <kebab-name> (depends: <track>|none)`.
     Tasks within a track run serially; independent tracks run in parallel at apply time. -->

## 1. Track: capability-spelling (depends: none)

- [x] 1.1 Done. Also collapsed `READOPTION_SANDBOX_FEATURE_CAPABILITIES` to `['lifecycle.readopt']`: every OTHER required/feature pair names two genuinely different capabilities (websocket vs interactive+exec, retained-read vs retained-source), and readoption only appeared to have a feature-level capability because the second entry was the same capability respelled
- [x] 1.2 `DEPRECATED_CAPABILITY_SPELLINGS` table in `parseCapabilities`. The unknown-capability error echoes what the operator WROTE rather than the normalized form
- [x] 1.3 **Two live copies deleted, not three.** `sandbox-core/capabilities.ts:143` (`hasDeclaredCapability` collapsed to `set.has`) and `sandbox-conformance/conformance.ts` (two sites, 351 and 448). The third — `sandbox-scheduler/src/scheduler.ts` — turned out to be in a package `pnpm-workspace.yaml` EXPLICITLY EXCLUDES: turbo knows 16 packages and rejects a `--filter` on it, it is never built/typechecked/tested, and it plus `sandbox-aio-local`, `sandbox-lifecycle`, `sandbox-workspace-git` have **zero importers**. An edit to it was made and then REVERTED — touching a package outside the build implies it is maintained, and removing the dead packages is its own decision. A fourth copy exists in a test (`apps/api/src/tasks/startup-recovery.test.mjs:209`); it re-implements the rule rather than calling it. proposal/design/brief corrected
- [x] 1.4 Two cases in `boxlite-boundary-regressions.test.mjs`: identical resolved set + deprecated string absent from it, and a near-miss typo (`lifecycle.readoptionn`) still rejected with the operator's own spelling echoed. Verified able to fail: removing the alias entry makes it red with `unknown capability: lifecycle.readoption`
- [x] 1.5 Deprecation noted under `BOXLITE_CAPABILITIES`; the two worked examples that TAUGHT the deprecated spelling now use the canonical one. That the README recommended it is why the input has to keep working

## 2. Track: capability-classification (depends: capability-spelling)

- [x] 2.1 `SANDBOX_PROVIDER_CAPABILITY_CLASSES: Readonly<Record<SandboxProviderCapability, 'operation' | 'feature'>>` is now the source
- [x] 2.2 All three derive via `capabilitiesOfClass`; KNOWN is now the mapping's key set by construction rather than a concatenation that happened to add up to the union. Same names, same types, member order preserved (object key insertion order) — zero consumer edits needed
- [x] 2.3 Three transcribed lists replaced by property assertions: KNOWN == operation ∪ feature, no capability twice (the readopt/readoption split was exactly that), the partition is disjoint and neither side empty, and every `*_CAPABILITIES` export names only classified capabilities. Also flipped the two alias-semantics assertions: `missingCapabilities(['lifecycle.readoption'], ['lifecycle.readopt'])` now correctly reports the requirement as MISSING
- [x] 2.4 26/26 packages green with the cache bypassed. Compile-error property verified directly: adding `lifecycle.hibernate` to the union without classifying it produces `TS2741: Property '"lifecycle.hibernate"' is missing … in type 'Readonly<Record<SandboxProviderCapability, …>>'` (then reverted). One more test encoded the removed contract and was rewritten: `provider-center.test.mjs` asserted a provider constructed IN-PROCESS with the deprecated spelling reads as `declared` — it now must throw, since bypassing the parse boundary is exactly how the alias would creep back. No live provider takes that path

## 3. Track: provider-identity (depends: none)

- [x] 3.1 **Resolved: DELIBERATE, not drift.** `ProviderSeamSchema.options` is a required-COVERAGE set — the reference-E2E config is REJECTED unless it supplies an environment for every seam named. Adding `cloud-http` would impose a new operational requirement, and an impossible one: cloud-http declares only `terminal.websocket` / `transcript.retained-read` / `lifecycle.readopt`, so it can neither materialize a workspace nor execute a command. Timeline settles intent — cloud-http shipped in 62d5ac1 (2026-06-25), nearly three weeks BEFORE the enum was written in a27356f (2026-07-14), so the author had it available and chose the pair; the `.max(2)` bound encodes the same. Rederiving these would have been a behaviour change disguised as a refactor
- [x] 3.2 New `packages/contracts/src/provider-family.ts` holds `SANDBOX_PROVIDER_FAMILIES`; the environment schema is now `z.enum(SANDBOX_PROVIDER_FAMILIES)`
- [x] 3.3 `z.enum([...SANDBOX_PROVIDER_FAMILIES, 'unknown'])` with the reason recorded on the declaration
- [x] 3.4 Both became named `as const satisfies readonly SandboxProviderFamily[]` subsets — so a family RENAME is now a compile error at both, while the deliberate omission survives — with the full reasoning (coverage-set semantics, cloud-http's capability set, the timeline) recorded once and cross-referenced from the other. The manifest's `.max(2)` now derives from the subset's length
- [x] 3.5 `knownProviderFamily` now narrows against `SANDBOX_PROVIDER_FAMILIES` instead of a three-way literal chain that looked exhaustive while silently rejecting any newly declared family
- [x] 3.6 Both member sets identical; 26/26 packages green with the cache bypassed. The diagnostics schema's member ORDER did change (`aio, cloud-http, boxlite` → `aio, boxlite, cloud-http`) exactly as the design predicted, and no test asserted it — so no test needed fixing

## 4. Track: conformance-participation (depends: capability-spelling)

- [x] 4.1 `SANDBOX_CAPABILITY_CONFORMANCE_COVERAGE`, total over the vocabulary, in a new `required-participation.ts`. Entries are NOT guesses — each family assignment is the family whose source actually branches on that capability (`conformance.ts:349` for the provider family, `:437-444/981-1040` for behavior, `diagnostic-conformance.ts` for diagnostic). **Design collision found and escalated:** three capabilities are DECLARED by shipped providers with no family covering them (`workspace.source.volume`/`.git` by AIO, `transcript.retained-source` by BoxLite), so the requirement as first written would have failed both providers on day one. User chose enumerated gaps; the spec requirement was amended to match BEFORE coding it
- [x] 4.2 `createConformanceParticipationLedger` + `resolveRequiredConformanceScenarios`. **Second collision, also recorded rather than papered over:** the spec said participation should be made "unexpressible", but the three provider suites build their fixtures in separate scopes, so a single call site is a large test restructure of its own. Rather than leave a SHALL the code does not meet — the exact disease this change treats — the requirement was reworded to the ledger form that IS built, with the stronger shape noted in the module
- [x] 4.3 All three wired. AIO's ledger uses the FULLY-HOOKED provider's declaration, since bare `createProvider()` advertises only two capabilities and would have understated the obligation. Two insertions initially split a `const … =` assignment in half (silently making the scenario list `undefined`); caught by the suites going red, both repositioned
- [x] 4.4 26/26 packages green, cache bypassed. Outcomes unchanged. The gaps now PRINT rather than being invisible: AIO reports `workspace.source.volume` / `.git`, BoxLite reports `resource.disk-size-gb`. That last one exposed a factual error in my own mapping comment ("no shipped provider declares it") — BoxLite's native mode resolves it into its declared set; corrected
- [x] 4.5 Dropping BoxLite's `command-output` participation fails with: `BoxLite declares capabilities requiring "command-output" (required by command.exec), but that family did not run.` Reverted

## 5. Track: gate-and-verify (depends: capability-spelling, capability-classification, provider-identity, conformance-participation)

- [x] 5.1 `scripts/provider-contract-parity-check.mjs`: flags a capability pair where one name contains the other within a namespace (the readopt/readoption shape), and any provider test that builds conformance suites without a participation ledger. Caught a flaw in my own first draft — the `KNOWN_DISTINCT_PAIRS` seed entry (`transcript.retained-read`/`-source`) could never fire, since neither contains the other. Emptied it rather than leave data that only looks load-bearing
- [x] 5.2 7 cases, all passing, both detectors exercised in both directions — including the exact shipped pair, so a gate that could not see `lifecycle.readopt`/`lifecycle.readoption` would fail here
- [x] 5.3 `pnpm test:provider-parity`, wired into `ci.yml` as a third gate. Zero violations at the time of wiring
- [x] 5.4 All three gates green, typecheck 23/23, tests 26/26. **One flake found and NOT papered over:** `packages/sandbox-provider-aio/test/aio-terminal-session-ownership.test.mjs` fails roughly 1 run in 3 under parallel turbo load on a wall-clock assertion (`AssertionError: elapsed=81`). Pre-existing and unrelated: `git status` shows the only file I touched in that package is `aio-diagnostic-conformance.test.mjs`, and the flake file runs 3/3 green standalone. Reported, not retried into silence
- [x] 5.5 Nominated `firecracker` in `SANDBOX_PROVIDER_FAMILIES` (then reverted; `git diff` clean). **One edit produced 3 compile errors, all in files this change never touched** — the web diagnostics panel's total `Record` over families, `guardrails.service.ts:3445`, and `task-provisioning-diagnostic-observer.adapter.ts:297`. That is the property: before, adding a provider meant hand-editing four independent enums with nothing pointing at them, and the compiler only complained downstream of whichever one you happened to edit. The two deliberate api-side SUBSETS correctly did NOT break — they are stated subsets, not stale copies
