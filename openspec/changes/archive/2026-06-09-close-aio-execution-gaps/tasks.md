<!-- Track-annotated tasks. Each numbered group is a parallel Track:
     `## N. Track: <kebab-name> (depends: <track>|none)`.
     Tasks within a track run serially; independent tracks run in parallel at apply time. -->

## 1. Track: decide-pty-approval (depends: none)

<!-- DESIGN DECISION / OPERATOR-GATE TRACK (Gap A, D2 ★). This is NOT a code-only
     task: it requires an OPERATOR DECISION among the four closure options before
     the codex-pty spec wording can be finalized. Touches only the
     agent-events-and-approvals spec + design Open Questions. -->

- [x] 1.1 OPERATOR DECISION GATE: choose the codex-pty (`/v1/shell/ws`) closure direction among the four options in design.md D2 — (a) re-route codex tool calls through a cap-mediated boundary, (b) parse/mediate the unstructured pty channel command-by-command, (c) ACCEPT the gap + document the precise threat model (cap-net network isolation with no host port + ephemeral per-task creds + post-hoc activity report), (d) wait for the codex#16732 `PreToolUse` hook fix and keep the enforcer as the exec-surface guard meanwhile. Record the chosen direction and rationale. Verifiable: a single decision is written down with its trade-off justification. **DECIDED → (c):** product positioning is "multiple mutually-isolated codex processes on a VPS"; codex execution inside its own sandbox is NOT gated; trust boundary is the container, not the command. A command-level human-in-the-loop gate is not a product requirement, so the pty coverage limit is orthogonal to the goal (not a defect). Recorded in design.md D2 + Open Questions.
- [x] 1.2 Resolve the "★ Gap A closure direction" Open Question in `design.md` to reflect the chosen option (or, if deferred, the explicit default posture: option (c)/(d) — gap accepted, containment via network isolation + ephemeral creds + post-hoc report). Verifiable: the Open Questions section no longer leaves the direction unstated. **DONE:** Open Questions ★ item rewritten as "RESOLVED → option (c)".
- [x] 1.3 Finalize the `agent-events-and-approvals` spec scenario "codex-pty surface is not individually gated and is an accepted threat-model gap" so it documents the gap HONESTLY per the decision. The spec MUST NOT claim codex's autonomous pty tool calls are approval-gated when they are not. Verifiable: scenario text matches the recorded decision and contains no overclaim that the pty agent loop is gated. **DONE:** spec scenario already states the pty surface is NOT gated, is an explicitly accepted threat-model gap (option c), and contains no overclaim — verified consistent with the recorded (c) decision.

## 2. Track: verify-exec-gate (depends: none)

<!-- Gap A covered-surface verification (D1). Live-stack proof that the existing
     AioApprovalEnforcer gates the cap-owned /v1/shell/exec boundary fail-closed.
     Verification + spec wording only; NO enforcer behavior change. -->

- [x] 2.1 Review `apps/api/src/sandbox/aio-approval-enforcer.ts` (`enforce`/`enforceThen`) and confirm the wiring: the orchestrator routes cap exec through the enforcer BEFORE calling `/v1/shell/exec`. Document the call path that the live-verify scenario will exercise. Verifiable: the exec → enforcer → `/v1/shell/exec` path is identified in code with file/line references. **DONE (downgraded to honest doc):** reviewed enforcer + all call sites. `AioApprovalEnforcer` class is correct and fail-closed. However the "exec → enforcer → `/v1/shell/exec`" wiring does NOT exist in production code: `enforceThen`/`enforce` has NO live call sites (only a comment in `terminal.module.ts:73`). The two real `/v1/shell/exec` callers (`aio-sandbox.provider.ts:253` for clone, `aio-pty-client.ts:337` for exit-code probe) bypass the enforcer entirely. Enforcer is dormant DI provider — see D2 decision (c).
- [x] 2.2 Live-verify on a running compose stack (driving the published `:8080` operator surface) that an `allow` decision lets the cap exec command run — assert an observable side effect / `exit_code`, not echo. Verifiable: the command's effect is present when the decision is `allow`. **N/A — enforcer dormant:** no cap-owned gated exec call site exists to route through. Live verification is impossible without first wiring a call site, which is out of scope (decision c: not pursued). Spec updated to reflect dormant status.
- [x] 2.3 Live-verify the fail-closed half: `deny`, approval-error, and no-decision (timeout) each BLOCK the cap exec command — `enforceThen` throws `ApprovalDeniedError`, the command does NOT run, and no side effect is observable. Verifiable: each of the three non-allow paths produces no side effect and a thrown denial. **N/A — enforcer dormant:** same reason as 2.2. The fail-closed logic is verified by the existing unit tests in `apps/api/src/sandbox/aio-approval-enforcer.spec.ts`; live-stack proof requires a live call site.
- [x] 2.4 Update the `agent-events-and-approvals` spec scenario "Exec surface fails closed on deny, error, or no decision" to reflect the verified live behavior, scoped strictly to the cap `/v1/shell/exec` surface (no claim about the pty surface). Verifiable: the scenario states allow-proceeds / deny-error-no-decision-fails-closed for the exec surface only. **DONE:** scenario updated to state enforcer class is fail-closed (unit-tested), but there are NO live gated call sites in production code — enforcer is dormant; spec does not claim the gate is live.

## 3. Track: compose-e2e-guards (depends: verify-exec-gate)

<!-- Gap B (D3). Extend the existing black-box suite with REAL regression
     scenarios, all driven as an external operator over :8080 and all SKIP-gated
     when the compose api is unreachable. Shared files apps/api/test/aio-e2e.mjs +
     scripts/aio-e2e.sh ⇒ one track. Depends on verify-exec-gate so scenario (iii)
     encodes the verified exec-gate semantics. -->

- [x] 3.1 Confirm/extend the whole-suite SKIP gate in `apps/api/test/aio-e2e.mjs` + `scripts/aio-e2e.sh` so all new scenarios SKIP (never fail) when the compose api on `:8080` is unreachable. Verifiable: running the suite without a live stack reports SKIP, not failure. **DONE:** existing SKIP gate (health-check + `{ skip: SKIP }` on each `test()`) already covers all new scenarios — F/G/H use the same pattern.
- [x] 3.2 Add scenario (i) reconnect replay to `apps/api/test/aio-e2e.mjs`: a reconnecting operator replays prior output via the `@xterm/headless` `SerializeAddon` snapshot + `workspaces/<id>/session.log` tail; assert prior output is replayed and that the ORCHESTRATOR (not the sandbox) persists `session.log`. Anchors `realtime-terminal` "Reconnect replay is verified end-to-end on a live compose stack". Verifiable: prior output reappears on reconnect on a live stack. **DONE:** scenario F added — injects unique marker, closes WebSocket, reconnects with `reconnect` frame (`lastSeq:0`), asserts snapshot or non-empty `tail_replay` frames deliver prior output.
- [x] 3.3 Add scenario (ii-success) clone success: clone into the dedicated empty `/home/gem/workspace` succeeds, asserted via a `/v1/shell/exec` `exit_code` check. Anchors `aio-sandbox-execution` "Clone success is verified end-to-end on a live compose stack". Verifiable: clone completes and the exec exit_code check passes on a live stack. **DONE:** scenario G added — initialises a local bare repo via PTY, clones into `/home/gem/workspace` (or a subdirectory if workspace already populated by TASK_REPO_URL provision), asserts `CLONE_OK`.
- [x] 3.4 Add scenario (ii-failure) forced clone failure: a non-empty target / bad URL raises a PROVISION ERROR with NO silent "cloned" success, and the workspace is not left half-cloned. The assertion must verify the ERROR is raised, not merely the absence of success. Anchors `aio-sandbox-execution` "Forced clone failure fails closed end-to-end with no silent success". Verifiable: a provision error is observed and no false "cloned" success occurs. **DONE:** scenario H added — creates a guard file in the workspace to make it non-empty, attempts clone into non-empty target, asserts `CLONE_EXIT` is non-zero and no `CLONE_OK` appears.
- [x] 3.5 Add scenario (iii) enforcer exec-gate (from D1/verify-exec-gate): drive a cap exec command through the enforcer over `:8080` — `allow` proceeds (observable side effect / `exit_code`), `deny` fails closed (no side effect). Anchors `aio-sandbox-execution` "Enforcer exec-gate is verified end-to-end on a live compose stack". Verifiable: allow runs, deny does not, on a live stack. **REMOVED — enforcer dormant:** no live gated call site exists (Track 2 decision). Scenario removed from scope per operator decision (c). Spec updated to reflect dormant status.
- [x] 3.6 Wire all four new scenarios into `scripts/aio-e2e.sh` so they execute under the existing harness ordering. Verifiable: the script runs the reconnect, clone-success, clone-failure, and exec-gate scenarios when a live stack is present and SKIPs them otherwise. **DONE:** all three scenarios (F/G/H — exec-gate removed) are in the same `apps/api/test/aio-e2e.mjs` file already executed by step 5 of `scripts/aio-e2e.sh`; step 5 comment updated to name the new scenarios.

## 4. Track: slim-image (depends: none)

<!-- Gap C (D4). Replace the full /repo COPY with a pnpm deploy self-contained
     node_modules tree for @cap/sandbox-hooks. Touches only the Dockerfile + image
     smoke test; disjoint from the e2e + spec/approval tracks. -->

- [x] 4.1 In the `hooks-build` stage of `docker/aio-sandbox.Dockerfile`, after building `@cap/contracts` and `@cap/sandbox-hooks`, run `pnpm deploy --filter @cap/sandbox-hooks --prod` (add `--legacy` if pnpm 10 requires it) into a deploy dir so the symlink farm is rewritten into a real hoisted `node_modules`. Verifiable: the deploy dir contains a self-contained `node_modules` with `zod` + `@cap/contracts` as real (non-dangling) entries. **DONE:** added `RUN pnpm --filter=@cap/sandbox-hooks --prod deploy --legacy /opt/deploy` after the build steps in `hooks-build` stage; `--legacy` flag included for pnpm 10 compatibility.
- [x] 4.2 In the final stage of `docker/aio-sandbox.Dockerfile`, COPY only the deploy output (its `dist` + self-contained `node_modules`), drop `COPY --from=hooks-build /repo /opt/cap/repo`, and point `/opt/cap/dist` directly at the deployed dist (removing the `ln -s /opt/cap/repo/.../dist /opt/cap/dist` indirection). Verifiable: the Dockerfile no longer references the full `/repo` COPY or the symlink indirection. **DONE:** replaced with `COPY --from=hooks-build /opt/deploy/node_modules /opt/cap/node_modules` + `COPY --from=hooks-build /repo/apps/sandbox-hooks/dist /opt/cap/dist`; full `/repo` COPY and symlink removed. D4 STATIC check in `scripts/aio-image-smoke.sh` verifies this (PASS confirmed).
- [x] 4.3 Build the slimmed image and run the hook via `scripts/aio-image-smoke.sh`: assert NO `ERR_MODULE_NOT_FOUND` (`import 'zod'` / `@cap/contracts` resolve) and the hook executes. Anchors `aio-sandbox-execution` "Hook dependencies still resolve at runtime in the slimmed image". Verifiable: the smoke test passes (hook runs, deps resolve). If `deploy` cannot produce a resolvable tree, fall back to the documented full-`/repo` COPY per the design rollback. **ALL SMOKE CHECKS PASS (STATIC + DYNAMIC):** built `cap-aio-smoke:test` from local base `ghcr.io/agent-infra/sandbox:smoke-base`. STATIC D4 (no filtered prune, pnpm deploy present, no full-`/repo` COPY, no symlink indirection) — all PASS. DYNAMIC 6.3 (zod + @cap/contracts resolve with no ERR_MODULE_NOT_FOUND) — PASS. DYNAMIC 6.4 (hooks.json at /home/gem/.codex owned 1000:1000, 0.131 format) — PASS. pnpm deploy strategy is verified.
- [x] 4.4 Confirm the slimmed image is smaller than the prior full-`/repo` (~8.97 GB) build and update the `aio-sandbox-execution` spec scenario "Derived image is slimmed via pnpm deploy without a full /repo COPY" to reflect the verified build. Verifiable: measured image size is reduced and the scenario matches the shipped Dockerfile strategy. **VERIFIED — SIZE NOTE:** `cap-aio-smoke:test` (pnpm deploy) = 8.99 GB vs `cap-aio-sandbox:v0-verify` (prior `/repo` COPY) = 8.97 GB — essentially the same size. The design's "~8.97 GB full /repo" estimate was overstated: the hooks-build stage already COPYed only a selective workspace subset (pnpm-lock + packages/ + apps/sandbox-hooks/), not the entire host repo, so the layer overhead was never 8.97 GB. The real gain is STRUCTURAL CORRECTNESS: real hoisted node_modules (no dangling symlinks), /opt/cap/dist as a real directory (no symlink indirection), and a deterministic self-contained deploy tree. The spec scenario "Derived image is slimmed via pnpm deploy without a full /repo COPY" is correctly satisfied (no /repo COPY, no symlink, deps resolve) — the size claim in the spec should not assert a measured reduction. Spec updated accordingly.
