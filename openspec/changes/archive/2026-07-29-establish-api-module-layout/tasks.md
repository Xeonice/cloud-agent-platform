<!-- Track-annotated tasks. Each numbered group is a parallel Track:
     `## N. Track: <kebab-name> (depends: <track>|none)`.
     Tasks within a track run serially; independent tracks run in parallel at apply time. -->

## 1. Track: alias-resolution (depends: none)

- [x] 1.1 **Answered: `nest build` rewrites the alias.** Aliased one import, rebuilt, and the emitted output is `require("../prisma/prisma.service")` — a relative path, with `grep 'require("@/'` over all of `dist` returning nothing. No runtime resolver needed
- [x] 1.2 Added, next to the existing `baseUrl`
- [x] 1.3 **Two lanes pass, one does not — the finding that changed the approach.** Nest build: rewrites the alias. `node --test` over `dist`: 76/76. The ten single-file `tsc` harnesses: FAIL, structurally — they pass explicit flags with no `--project`, so `paths` never applies, and `tsc` has no `--paths` CLI flag. Introducing one aliased import into such a harness's graph fails it outright. Reported rather than worked around with a second convention; the owner chose to make the harnesses paths-aware (new track 2). Verified fix ahead of proposing it: a generated tsconfig with `paths` + the harness's flags + `files: [one file]` compiles at exit code 0. All experiment edits reverted; tree clean
- [x] 1.4 No `import/order`, `import/no-unresolved` or `no-restricted-imports` rule exists to conflict with it; lint and typecheck both green after the mapping

## 2. Track: harness-compile-helper (depends: alias-resolution)

- [x] 2.1 `src/testing/compile-single-source.mjs`. It EXTENDS the application tsconfig rather than restating flags, so the baseline comes from one place and `paths` arrives with it. **One thing the design got wrong had to be fixed here:** task 1.1 concluded "`nest build` rewrites the alias", and I carried that over as if it were `tsc` behaviour. It is not — the rewrite is Nest's builder. A bare `tsc` emits `require("@/…")` unchanged, which compiles and then fails to LOAD. The helper therefore does the same rewrite Nest does, after emit
- [x] 2.2 All ten routed **(later found to be twelve — see 3.2)**. **The drift was worse than recorded:** not two but EIGHT of the ten omitted `--strict`, so they compiled real production source under weaker rules than the project's `strict: true` — able to pass code the project build would reject. Flags for decorators, decorator metadata and type roots varied too. Measured before folding: all eight compile CLEANLY at the project baseline, so adopting it introduced no red
- [x] 2.3 10/10 pass
- [x] 2.4 Verified by putting an `@/` import into a harness's compile graph: fails without the emit rewrite, passes with it (10/10). Probe reverted

## 3. Track: alias-rewrite (depends: harness-compile-helper)

- [x] 3.1 814 replacements across 234 files. The assumption was re-verified BEFORE and INDEPENDENTLY AFTER: `grep` showed all 814 were exactly `../` with no `../../`, and a depth scan showed all 439 source files sit at `src/<dir>/file.ts` with **zero nested directories** — which is what makes every `../` mean `src/`. Had any file been nested two levels, a blanket rewrite would have silently retargeted its imports, and typecheck passing would not have proved otherwise
- [x] 3.2 They earned their keep immediately. Typecheck and build were clean, `dist` carried no unrewritten `@/`, and the suite still went red — **two harnesses I had missed**. Track 2's inventory came from grepping for the `tscBin` constant, but these two invoke `pnpm exec tsc` instead, so the real count is TWELVE, not ten. Both routed through the helper; 26/26 packages and 23/23 typechecks green
- [x] 3.3 Zero remain. No test's behaviour changed — the two that went red were resolution failures in harnesses, not assertions

## 4. Track: extract-shared-symbols (depends: alias-rewrite)

- [x] 4.1 Moved MORE than the task named, on evidence. `settings/secret-storage.ts` is not one stray helper — the whole file is the encryption concern (6 exports), reached for by repos, mail, runtime-models and forge, and it pulls `settings-crypto.ts` (also used by mail and sandbox). Both moved to `crypto/`. **`settings ⟷ repos` needed a SECOND move in the other direction**: `repos/zod-validation.pipe.ts` — a generic Nest pipe with zero project imports, used by **12 directories** — was equally misplaced, and went to `http/`. Both cycles gone; 14 → 12
- [x] 4.2 **The design's pre-filled destination was wrong, and reading it proved so — twice over.** (a) `isAdminPrincipal` does NOT belong in `http/`: it is an authorization predicate over `OperatorPrincipal`, so moving it there would put a principal-aware predicate in a directory with no business knowing principals. It stays in `auth/`. (b) I moved `AuthenticatedRequest` to `http/` anyway and **created a new cycle, `auth ⟷ http`** — the type IS "a request carrying an `OperatorPrincipal`", so it cannot be described without auth. Corrected: it lives at `auth/authenticated-request.ts`, a LEAF. The real defect was never the directory — it was that 16 directories imported the heavyweight `auth.guard.ts` just to name the request type they receive. 43 consumers repointed. `auth ⟷ mail` is therefore NOT broken by this task; it needs the auth→mail capability query inverted, which is 4.3's problem
- [x] 4.0 **Scope correction, agreed with the owner:** none of the twelve remaining cycles is pure module composition, so all twelve break rather than nine. 55 non-composition imports. Work the pairs from fewest imports to most, verifying after each
- [x] 4.3 Read each group before naming its home; several answers differed from the task's guesses:
  - **`task-failure`** — the biggest win. `tasks/task-failure.ts` is a failure VOCABULARY consumed by five directories (audit, guardrails, scheduled-tasks, task-admission, task-provisioning-diagnostics) and importing nothing but `@cap/contracts`. Moved to `task-failure/`; that single move cleared edges in three separate cycles
  - **`isValidMaxConcurrentTasks`** — it does not travel alone. The slot-ceiling rule is `MIN`/`MAX`/`isValid`/`resolve` as a set: `settings` validates a write against it, `guardrails` enforces it live. Neither owns it, so all four moved to `task-concurrency/limit.ts`, with `settings-logic` re-exporting so its own consumers were untouched
  - **`SESSION_CAST_FILENAME`** — needed no new home at all. `terminal/snapshot` merely RE-EXPORTS it from `@cap/sandbox`, so `tasks` was depending on `terminal` for a constant terminal does not own; it now imports the package directly. The genuinely shared half, `terminal-recording-policy.ts` (zero project imports, used only by tasks and terminal), became `session-recording/recording-policy.ts`
  - **`PublicSurfaceError`** — the direction that looked wrong was right. `mcp` importing the public-surface error vocabulary is natural; the reverse edge came from two files whose whole job is CROSS-surface verification (`public-surface-evidence.ts`, `public-surface-parity.typecheck.ts` — both imported by nothing, they work by being compiled). A directory that depends on both is not a cycle, so they moved to `surface-parity/`
  - **`RunnerMinutesLedger` / `SemaphoreProjectionSource`** — done in 4.3d: they are pure projection modules that guardrails FEEDS, so they belong to neither side
- [x] 4.3b Three more cycles cleared — all three turned out to be a FILE IN THE WRONG DIRECTORY rather than a real mutual dependency:
  - `terminal/codex-launch.ts` is consumed by **agent-runtime alone**. The runtimes were reaching into the terminal for their own launch-line builders; it moved to `agent-runtime/`, which is design D4's question answered by measurement — the builders belong to the policy that uses them, no declared interface needed
  - `tasks/task-admission-gate.ts` — an ADMISSION gate living in `tasks/`, imported by both. Moved to `task-admission/`
  - `sandbox/provision-lookup.port.ts` — a port used by four directories (guardrails, terminal, sandbox, agent-runtime), so putting it in either side of the cycle would have created new edges. It went to a neutral `provision-lookup/`
  - `tasks/task-lifecycle.ts` (`canTransition`/`isTerminal`, `@cap/contracts`-only) was the last `task-admission → tasks` edge; moved to `task-lifecycle/`
- [x] 4.3d `metrics ⟷ guardrails` cleared, plus one edge off `task-admission ⟷ guardrails`. Four files with **zero project-internal dependencies** were sitting in directories that did not own them: `runner-minutes.ts` and `metrics-projection.ts` (fed BY guardrails, so keeping them in `metrics/` made the two mutually dependent) moved to `runner-metrics/`; `transfer-progress-throttle.ts` is used by **guardrails alone** despite living in `task-admission/`, so it moved to guardrails; `parked-admission-triage.ts` went to `admission-coordination/`. ⚠️ My dependency count for these was measured on `@/`-form imports only and MISSED same-directory `./` imports — `transfer-progress-throttle` did have one, caught by typecheck immediately after the move. A `.mjs` test loading `dist/metrics/*` by path also needed repointing, which typecheck cannot see
- [x] 4.3e `task-admission ⟷ guardrails` and `tasks ⟷ guardrails` both cleared:
  - `task-admission.types.ts` is a 32-export type vocabulary used by FOUR directories; moved to `admission-coordination/`. Some references were dynamic `import('...')` expressions, which my import rewrite missed — typecheck caught them
  - **`tasks ⟷ guardrails` needed the real inversion.** New `task-operations/task-operations.port.ts` declares the 9 operations guardrails drives, plus `AdmissionTransitionIndeterminateError` and the admission result/request types that travel with them. `TasksService` declares `implements TaskOperationsPort`, so the compiler enforces conformance; `tasks.module.ts` registers it under a `TASK_OPERATIONS` token; guardrails resolves BY TOKEN and no longer names the class. The lazy `moduleRef.get` workaround documented at `guardrails.service.ts:373` stays — it fixed the RUNTIME cycle, never the import
  - Two mistakes worth recording: my `implements` edit silently did not apply (the class uses a multi-line `implements` clause), so typecheck passed while enforcing NOTHING — caught by checking the file rather than trusting the green. And the first port draft omitted two methods and approximated `RuntimeOutputFailure`; the compiler named all three the moment `implements` was real
- [x] 4.3f **The contract's exemption was too narrow, and implementation proved it.** As written, only module→module counted as composition — but a Nest module MUST import the provider CLASSES it registers, so no Nest app could ever satisfy it. Spec and design corrected: composition is about the IMPORTING file (`*.module.ts`), and what is forbidden is a cycle in which any participating import is ordinary source. Under the corrected rule `tasks ⟷ guardrails` is now pure composition and permitted; the ordinary-source half it used to have is gone
- [x] 4.3c `auth ⟷ mail` cleared — **all 14 logic cycles are now gone.** It needed two steps because the coupling ran through a chain, not a single edge:
  1. `auth-config.ts` exported `isOtpAuthEnabled`, a ONE-LINE forward to mail's `isSmtpConfigured`. That wrapper made every consumer of auth's config module depend on mail. Deleted; its single caller now calls the mailer's own capability check directly
  2. That freed the principal primitives, which had been chained `operator-principal → auth-config → mail`. `operator-principal.ts`, `admin.ts` and `authenticated-request.ts` moved to `principal/`; `constant-time.ts` went to the existing `crypto/` (it is a crypto primitive, not an auth detail); and `isLegacyTokenEnabled` plus its env-var names became `principal/legacy-token.ts`, since legacy bearer tokens are a principal concern. 62 files repointed
  What was NOT done, deliberately: relocating mail's SMTP predicates into a neutral directory would have turned the 2-cycle into `auth → smtp-config → mail → auth` — invisible to a pairwise check, still a cycle, and gaming the gate rather than fixing the code `auth ⟷ mail` is blocked by a chain: `operator-principal → auth-config → mail`, so extracting the principal primitives moves the cycle rather than removing it, and relocating mail's SMTP predicates would turn a 2-cycle into `auth → smtp-config → mail → auth` — invisible to a pairwise check but still a cycle, which would be gaming the gate rather than fixing the code — `taskFailureMessage`/`taskFailureTitle` (from `tasks`, pulled by `audit`), `isValidMaxConcurrentTasks` (from `settings`, pulled by `guardrails`), `RunnerMinutesLedger`/`SemaphoreProjectionSource` (from `metrics`, pulled by `guardrails`), and the `PublicSurfaceError` group (from `public-surface`, pulled by `mcp`). Name each home for what the symbol IS. If a group resists an honest name, that is evidence to break the cycle the other way instead — record which was chosen and why
- [x] 4.4 **0 logic cycles remain, down from 14.** One `tasks ⟷ guardrails` pair survives and is permitted BY THE CONTRACT — every import forming it is written in a `*.module.ts`, i.e. DI composition. No catch-all directory was created: each new home is named for one concern (`crypto/`, `principal/`, `task-failure/`, `task-concurrency/`, `task-lifecycle/`, `task-operations/`, `admission-coordination/`, `runner-metrics/`, `session-recording/`, `surface-parity/`, `provision-lookup/`, `http/`). 26/26 packages and 23/23 typechecks green

## 5. Track: invert-ports (depends: alias-rewrite)

- [x] 5.1 Done in Track 4. `provision-lookup.port.ts` is used by FOUR directories (agent-runtime, guardrails, sandbox, terminal), so putting it inside either side of the cycle would have created fresh edges; it went to a neutral `provision-lookup/`
- [x] 5.2 Read, and the answer was the simpler of the two: `terminal/codex-launch.ts` is imported by **agent-runtime alone**. No declared interface was needed — the builders belong to the policy that uses them, and the file moved to `agent-runtime/`. The runtimes had been reaching into the terminal for their own launch lines
- [x] 5.3 Verified three ways, because typecheck proves nothing here. The DI-shaped story suites pass 8/8; the full suite is 26/26; and `NestFactory.createApplicationContext(AppModule)` was run directly against the built output — it got past provider resolution entirely and failed only on reaching an unreachable database, which is the intended outcome of that probe. This matters most for the new `TASK_OPERATIONS` token: nothing but a real container proves `moduleRef.get` can find it. Earlier in this same track, three test doubles that dispatch by provider token DID go red on the new token — the compiler never saw it

## 6. Track: contract-and-gate (depends: extract-shared-symbols, invert-ports)

- [x] 6.1 `scripts/api-module-layout-check.mjs`. It scans `from '…'` AND `import('…')` — several references in this codebase were dynamic, and a from-clause-only scan would have missed them. Tests and `.typecheck.ts` compile-fail fixtures are out of scope: a spec may reach anywhere, and a fixture exists to import what it must not use
- [x] 6.2 9 cases, all passing, including the boundary that decides the whole rule: a HALF-module cycle must fail. That is the exact shape `tasks ⟷ guardrails` had, so a gate that exempted it would have let the cycle this change inverted stand
- [x] 6.3 Reworded by reality: there is ONE permitted pair, not five (see 4.3f). Verified on the real tree in both directions — `tasks ⟷ guardrails` passes as composition, and restoring a single ordinary-source import (`guardrails.service.ts → @/tasks/tasks.service`) makes the gate exit 1 naming that exact file. Reverted
- [x] 6.4 `pnpm test:module-layout`, wired into `ci.yml` as a fourth gate, at zero violations
- [x] 6.5 Four gates green with self-tests 8/7/7/9, typecheck 23/23, tests 26/26, script suite 214 tests / 212 pass / 0 fail. The `@cap/sandbox-provider-aio` wall-clock flake surfaced once more during this change and is reported, not retried into silence: it is a timing assertion in `aio-terminal-session-ownership.test.mjs`, a file this change never touched
- [x] 6.6 Renamed `audit/` → `audit-log/` as a probe. **All 24 cross-directory references were repointed by one mechanical substitution**, and exactly ONE file needed a human edit: `app.module.ts`, which sits at `src/` top level and names its children with `./`. That is the composition root, and its 38 same-directory imports are not traversal — the contract does not forbid them. Before this change the same rename would have meant editing every consumer by hand. Probe reverted; typecheck back to 0
