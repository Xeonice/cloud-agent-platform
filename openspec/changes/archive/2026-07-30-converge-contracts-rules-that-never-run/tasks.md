<!-- Track 0 is first because two of this change's own scope decisions would
     otherwise be read off an instrument that is measurably wrong. Track 2 cannot
     start until Track 1 has re-measured with the fixed instrument — the ≈60
     figure in the design is a text probe's upper bound, not the scope. -->

## 0. Track: fix-the-instrument (depends: none)

- [x] 0.1 Fix the schema/type pair self-reference in `scripts/contracts-shared-export-check.mjs`. `isComposedInto` skips only an export's OWN declaration line, so the pair line `export type X = z.infer<typeof XSchema>` counts as composition and marks every schema that has an inferred type reachable before the pair rule is consulted. Composition must exclude a reference that is the twin's own declaration. Prove it with the probe that exposed it: a `GateProbeDeadPairSchema` + `GateProbeDeadPair` pair nothing uses currently passes with exit 0.
  - requirements: ["monorepo-foundation/an-export-nothing-can-reach-shall-fail-the-build"]
  - surfaces: ["ci"]
  - verify: "workflow-gates"
- [x] 0.2 Add `scripts/` to the consumer set. `consumerRoots()` walks only `apps/*` and `packages/*` whose manifest declares a dependency on the contracts package; 16 files under `scripts/` reference contracts, several through deep `dist/` paths the import regex would not match either. Follow `test-discovery-check.mjs`'s `REPOSITORY_TEST_DIRS` shape — the same problem already has a solution in this repository. **Took three fixes, not one.** Scripts import through a relative path into the build (`'../packages/contracts/dist/credential-prefix.js'`), so the import matcher and the pre-filter both had to accept that specifier as well as the package name. And the walk immediately read two files' PROSE as code: this gate's own paragraph explaining namespace imports contains one as an example, and its test file builds fixtures out of import statements. Comments are now stripped before matching (prose is not code — `isComposedInto` always did this and this did not), and the test file is named in a `NOT_REALLY_IMPORTS` list, because a text scan cannot tell a fixture from an import and any file that tests import scanning contains both. Consumers 7 → 8, imported exports 501 → 538.
  - requirements: ["monorepo-foundation/an-export-nothing-can-reach-shall-fail-the-build"]
  - surfaces: ["ci"]
  - verify: "workflow-gates"
- [x] 0.3 Re-derive the EXCEPTIONS list against the fixed scan and **delete every entry whose stated reason measurement contradicts, rather than rewording it**. At least one is already known false: `startsWithReservedPrefix` is kept on the ground that "a test reimplements it inline", while `scripts/legacy-token-prefix-collision.test.mjs:18` imports it properly from `packages/contracts/dist/credential-prefix.js` — a file the scan cannot currently see. Report the number of entries removed; an exception's reason is its whole content. **Removed 3 of 17.** `startsWithReservedPrefix` is the one 0.3 predicted: its reason was false AND false about a file the scan could not see, and once `scripts/` joined the walk the export stopped being unreachable at all. `NotifyLevel` / `NotifyLevelSchema` were never needed — `NotifyPayloadSchema` is composed from them — and an exception nothing needs reads as a finding that was adjudicated when it never arose. **The gate is now RED with 28 exports (14 pairs) and that is deliberate**: the list is dominated by `auth-account`, which is dead *because* `accounts.service.ts` restates it, and design D3 forbids excepting or deleting before converging. Track 2 converges, Track 3.2 adjudicates the remainder, and 4.3 requires green.
  - requirements: ["monorepo-foundation/an-export-nothing-can-reach-shall-fail-the-build"]
  - surfaces: ["ci"]
  - verify: "workflow-gates"
- [x] 0.4 Extend `scripts/contracts-shared-export-check.test.mjs` with the two cases Track 0 fixes: a fully dead schema/type pair fails naming both halves, and an export imported only from `scripts/` is not reported dead. Both must be shown to fail before the fix and pass after — the previous change's task 4.4 proved its gate with the one shape the gate already handled, and that is how the pair hole survived. **Five cases, four of which are red on the pre-fix tree**: the self-vouching pair (written across three lines, because a continuation line is exactly what a line-level skip could not see), a commented import not counting as one, an import through the built package path counting, and `scripts` appearing in the consumer list. The fifth — a genuine composition still seen across continuation lines — passes both before and after on purpose; it is the guard that the pair fix did not trade one silent hole for another. **`the repository is clean under the gate` is currently RED with 28 exports and that is the intended state**, per D3: the list is dominated by `auth-account` exports that are dead because `accounts.service.ts` restates them, so converging comes first and 4.3 requires green.
  - requirements: ["monorepo-foundation/an-export-nothing-can-reach-shall-fail-the-build"]
  - surfaces: ["ci"]
  - verify: "workflow-gates"

## 1. Track: measure-execution (depends: fix-the-instrument)

- [x] 1.1 Build the execution-reachability scan: a contract schema RUNS when non-test production code parses it (`.parse` / `.safeParse` / `.parseAsync` / `.safeParseAsync`, `ZodValidationPipe`, `UsePipes`) or when a schema that runs is composed from it. Model indirection by DECLARATION per design D2, not by inference — `/v1` parses through `public-v1-operation.ts:436-455` (`operation.input.params|query|headers|body.parse`) on an entry fetched from the operations table, so eight schemas a text probe reports dead are in fact executed. A list of declared indirection points, each with its file:line and the entries it covers. `scripts/contracts-executed-schema-check.mjs`. The declared `/v1` point works: `V1CreateTaskRequestSchema`, `V1ListQuerySchema`, `PublicV1IdParamsSchema` and `PublicV1EventHeadersSchema` all read as executed, where a text probe called them dead. One category error caught in passing — a FUNCTION whose name ends in `Schema` (`composePublicInputWireSchema` builds one) was landing on the list of rules nothing enforces; a schema is a `const`, and a factory is not a rule.
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes"]
  - surfaces: ["ci"]
  - verify: "workflow-gates"
- [x] 1.2 Report test-only execution separately, as the import gate already does for test-only reachability. A schema parsed only by `packages/contracts/src/*.test.mjs` proves the schema parses; it proves nothing about the bytes production sends, which is the drift the scan exists to catch.
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes"]
  - surfaces: ["ci"]
  - verify: "workflow-gates"
- [x] 1.3 Re-measure with the real instrument and record the number. **The design's ≈60 is a text probe's upper bound with at least 8 known false positives — do not carry it forward as the finding.** This measurement, not the probe's, decides Track 2's scope. **390 schemas, 341 executed by production code, 49 never run — 7 of those parsed only by tests.** The probe's 68 was 19 too high, almost exactly the /v1 indirection it could not see. By module the list is led by `auth-account` (12), which is the accounts/OTP restatement Track 2 converges; then `task-provisioning-diagnostics` (7), `mcp-token` (5), `notifications` (4, already an accepted exception on the import gate) and `asciicast` (3).
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes"]
  - surfaces: ["ci"]
  - verify: "workflow-gates"
- [x] 1.4 Prove the scan fires against **the shape of the defect it targets** (design D7): the throwaway must be a schema that IS imported and IS composed into a live type and is never parsed, because that is what `SmtpConfigReadSchema` looked like on the day it was wrong. A probe that is merely unimported proves only that the import gate still works. **Proved, and the proof doubles as evidence the two gates are not redundant**: a `GateProbeUnparsedSchema` imported by `runtimes.controller.ts` and composed into a live exported type is caught by THIS gate (49 → 50, named, exit 1) and is invisible to the import gate (zero matches). Removing it returns the count.
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes"]
  - surfaces: ["ci"]
  - verify: "workflow-gates"

## 2. Track: converge-violations (depends: measure-execution)

<!-- The three confirmed violations. Each changes what is enforced, so each needs
     evidence against the PRE-change tree, not a typecheck. -->

- [x] 2.1 `AdminRevealResponseSchema`: model the "no credential to reveal" arm in the contract. `admin-reveal.controller.ts:54,80` returns `{}` on two of three paths against a contract declaring `email` and `password` both required — the third instance of the defect this programme has now found three times. Contract first, parse second (design D5): adding the parse first turns a working endpoint into a 500 on its majority path. Done as a union of `AdminRevealCredentialResponseSchema` and a STRICT empty object. **The api was right and the contract was incomplete** — `admin-reveal.controller.ts` documents the empty arm as a security property ("so a probe cannot distinguish 'consumed' from 'never generated'") and returns it on two of three paths, while the contract declared only the success arm. Its local `AdminRevealCredential | Record<string, never>` was the correct shape in the wrong place and is deleted.
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes"]
  - surfaces: ["contracts"]
  - verify: "contracts-registry"
- [x] 2.2 Give `AdminRevealResponseSchema` its call site, parsing on the way out as `SmtpController` and `RuntimesService` now do. Verify against `scripts/boot-smoke.sh:125`, which probes this endpoint before auth — that probe is both the risk and the evidence. The handler is split so the parse wraps EVERY return path rather than one of them. Exercised against compiled `dist`: the no-credential path returns `{}` and parses; a full credential parses; a stray key is rejected; and half a credential (`{ email }` with no password) is rejected, so the union did not become a shape that accepts anything.
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes"]
  - surfaces: ["contracts"]
  - verify: "contracts-registry"
- [x] 2.3 Delete the five local DTOs at `apps/api/src/accounts/accounts.service.ts:197-245` (`AccountRoleSchema`, `CreateAccountSchema`, `SetEnabledSchema`, `ResetPasswordSchema`, `AssignRoleSchema`) and use the contract's, restoring `PasswordSchema`'s `.max(200)` — a bound whose comment states it exists so a single field cannot exhaust the argon2 hasher, and which the local restatement drops. `AdminCreateAccountRequestSchema` and `AdminResetPasswordRequestSchema` have zero references anywhere in `apps/api/src` or `apps/web/src` today. Re-exported under the local names the controller already uses, so the seam is one file rather than a rename across every call site. `MIN_PASSWORD_LENGTH` goes with them — the floor is the contract's now too.
  - requirements: ["monorepo-foundation/a-shared-type-shall-have-exactly-one-declaration"]
  - surfaces: ["contracts"]
  - verify: "contracts-registry"
- [x] 2.4 Delete the local `OtpRequestSchema` / `OtpVerifySchema` at `apps/api/src/auth-otp/otp.controller.ts:24,28` and use the contract's, restoring `OtpCodeSchema`'s `/^\d{6}$/` where the local declares `code: z.string().min(1)`. The file already carries the TODO for this. **Three changes, not two**: `EmailSchema` is `.trim().min(1).email(...)`, so a malformed address becomes a 400 AND a padded one is normalised instead of failing the account lookup silently. Recorded because a transform is a behaviour change even when it is an improvement. The uniform-response property survives — a 400 for input that could not name any account does not distinguish a known address from an unknown one.
  - requirements: ["monorepo-foundation/a-shared-type-shall-have-exactly-one-declaration"]
  - surfaces: ["contracts"]
  - verify: "contracts-registry"
- [x] 2.5 Verify the two tightened surfaces against the PRE-change tree: a 201-character password and a five-digit OTP code must be ACCEPTED before and REJECTED after. A case that passes on the old code is not evidence. **Measured both ways**: on the pre-change tree a 201-character password (create AND reset) and a 201-character display name were all ACCEPTED; on this tree all three are REJECTED, while the 200-character forms still pass. The OTP code and email tightenings verified the same way. **Record in the proposal's own words that these are drifts between a declared rule and the enforced one, not live vulnerabilities** — the account routes are admin-gated and the OTP code is api-generated, and overclaiming is the failure mode this programme keeps catching in itself.
  - requirements: ["monorepo-foundation/a-shared-type-shall-have-exactly-one-declaration"]
  - surfaces: ["contracts"]
  - verify: "contracts-registry"
- [x] 2.6 Add the wire-compatibility fixture the surface-impact declaration requires. `runtimeWireBehavior` is `changed` because two request surfaces begin rejecting inputs they accepted, and the repository's own metadata gate demands a fixture for that — correctly. The fixture pins, per tightened field, the input that was accepted before and is rejected now (a 201-character password, a five-digit OTP code) and the input that must keep working unchanged (a 200-character password, a six-digit code), so a later relaxation or a further tightening both fail it. This is the artifact that makes 2.5's claim checkable by someone who was not here. `apps/api/test/tightened-request-surfaces.test.mjs`, 9 cases. Beyond the edges it also pins what must NOT have moved: the `.min(8)` floor, the cross-field rule that a password-credential account without a password is a 400 (a convergence that dropped the `.refine` would have created credential-less accounts), the role/enabled bodies, and that rejecting a malformed address still cannot be used to probe account existence.
  - requirements: ["monorepo-foundation/a-shared-type-shall-have-exactly-one-declaration"]
  - surfaces: ["contracts"]
  - verify: "contracts-registry"
- [x] 2.7 Converge the mcp-token list envelope on the CONSOLE side (design D3): `apps/web/src/lib/api/real.ts:1467` declares `ListMcpTokensResponse` as a bare array while the api (`mcp-tokens.controller.ts:71`) and the contract (`mcp-token.ts:115`) both say `{ tokens }`. Unlike `RuntimeReadiness`, two of three parties already agree, so the console moves and the wire does not. Delete the both-branch tolerance at `real.ts:1499` — leaving it preserves the ambiguity, exactly as it did at `real.ts:776`. Done, and `McpTokenSummary` (field-for-field identical to the contract's `McpTokenListItem`) is deleted with it. **The api also parses on the way out now**, which gives `McpTokenListResponseSchema` the call site it never had. **Two tests had encoded the drift and both were fixed rather than accommodated**: `mcp-server-section.test.ts` served a BARE ARRAY the api has never sent, and passed only on the tolerance being deleted; `auth.guard.spec.ts` used `id: 'tok-1'` where the contract declares a uuid and Prisma generates one (`@default(uuid())`), so it was asserting on data the database cannot produce.
  - requirements: ["monorepo-foundation/a-shared-type-shall-have-exactly-one-declaration"]
  - surfaces: ["contracts"]
  - verify: "contracts-registry"

## 3. Track: settle-false-declarations (depends: measure-execution)

- [x] 3.1 Delete `AUTH_TOKEN_PUBLIC_ENV_VAR` (design D6). It declares `'NEXT_PUBLIC_AUTH_TOKEN'`, a name from the Next.js console that no longer exists; the string appears in exactly three places repository-wide — its own declaration, its own doc comment, and the exception reason written for it last week — while the console reads `VITE_AUTH_TOKEN` at `apps/web/src/lib/config.ts:238`. Correcting it would keep a browser-only env-var name in a package defined as what both sides share. Done; the exception written for it last week goes with it.
  - requirements: ["monorepo-foundation/an-export-nothing-can-reach-shall-fail-the-build"]
  - surfaces: ["contracts"]
  - verify: "contracts-registry"
- [x] 3.2 Adjudicate every remaining unexecuted schema from 1.3, one of three ways and never silently: give it a production call site, delete it, or record it as an exception with a reason. `Xterm550ResponseProfileSchema` is the interesting one — its doc says a drifted descriptor "is not negotiable" and nothing enforces that on the negotiation path, so it is a rule about a protocol between api and web that only its own test executes. **Adjudicated 26 schemas in parallel, each verdict then attacked.** Outcome: 5 call sites added (`OtpRequestResponseSchema`, `McpTokenMintResponseSchema` — the one response in the repository carrying a raw `mcp_` token, `McpTokenRevokeResponseSchema`, `McpTokenListResponseSchema`, `TestSmtpConfigResponseSchema` via a helper because the handler has four return paths); 2 deletions (`WriteLeaseMapSchema`, whose singular form IS used and whose map form the frame protocol never adopted; `TaskProvisioningDiagnosticsRequiredCapabilitySchema`, a second `z.enum` derivation of a vocabulary already declared once as an `as const` array that production enforces by membership — the exported type now derives from the array); 18 exceptions, each with a checkable reason. **The adjudication also found three defect classes in the instrument built in Track 1, and fixing them removed 15 false positives (33 → 18)** — see 3.3.
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes"]
  - surfaces: ["contracts"]
  - verify: "contracts-registry"
- [x] 3.3 Check each adjudicated schema for a live violation before choosing. This is the step that found `AdminRevealResponse`: the question is not "is it used" but "does what we send today satisfy it". A schema given a call site while something already violates it turns a silent drift into a 500. **Two violations found, and both are excepted rather than wired for exactly that reason.** `AuditQuerySchema` declares `limit: z.number()` while HTTP query params arrive as strings, so `GET /audit/events` parses a file-local restatement with `z.coerce.number()` whose own comment admits it "mirrors the contracts AuditQuerySchema but coerces limit" — the contract needs the coercion arm before it can be the thing parsed, and that is a third request surface this change did not scope. `ForgeConnectionSchema` is violated on an input-dependent path. **Three defect classes in this change's own instrument, all found by the adjudication rather than by me:** (1) the scan never read `packages/contracts/src` itself, so a schema parsed inside an exported in-package helper that production calls — `parseAsciicastEvent` at `asciicast.ts:78`, reached from `terminal.gateway.ts` and from the console through `parseCast` — read as never-run; (2) composition propagated only schema-to-schema, so it stopped at the first un-suffixed intermediate (`RuntimeModelErrorBaseShape`, `DiagnosticEventIdentityFields`) and reported a dozen schemas dead while the union they build IS parsed; (3) the same in-package-helper blind spot covered `evaluateTaskAdmissionV2Gate`. Fixed by propagating through every declaration and by counting an in-package parse helper that a production file imports — the second stated with a `calledBy` file:line so the entry is falsifiable. **33 unexecuted → 18, and the gate reporting a schema BOTH as test-only and as never-run was the self-contradiction that exposed the first one.**
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes"]
  - surfaces: ["contracts"]
  - verify: "contracts-registry"

## 4. Track: gate-and-verify (depends: converge-violations, settle-false-declarations)

- [x] 4.1 Wire the execution gate as `pnpm test:contracts-executed` and a CI step beside `test:contracts-shared`, with the same self-testing shape. `pnpm test:contracts-executed`, 7 cases.
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes"]
  - surfaces: ["ci"]
  - verify: "workflow-gates"
- [x] 4.2 Delete `--test-force-exit` from `test:compiled` and `test:suite` in `apps/api/package.json` (user decision D5), and add the missing `timeout-minutes` to the `typecheck-lint` job in `.github/workflows/ci.yml`, which has none today and would inherit GitHub's 360-minute default on a hang. Measured: 1599/1599/1596/1597 with the flag on one unchanged tree, 1599 three times without; 20-41s unforced against 16-17s forced. Nothing else from the test-runner candidate — its larger proposal was refuted on measurement. **The claim is now checkable: three consecutive `test:compiled` runs report 1599 every time**, where the same command reported 1599/1599/1596/1597 with the flag. `timeout-minutes: 30` added to `typecheck-lint`, the only job in the file without one.
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes"]
  - surfaces: ["ci"]
  - verify: "workflow-gates"
- [x] 4.3 Run the full verification: build, every package suite, typecheck and lint across api/contracts/web, and all repository gates. Compare counts against `81a115d` — api compiled 1599/1595/4, src 293/293, suite 17/16/1, contracts 236/236, web 613 across 82 files, `test:scripts` 236/234/2 — and account for every difference. With 4.2 landed the api figure is finally comparable run to run, which is the point of carrying it. **All green.** `turbo build` 14/14, `turbo typecheck lint` 37/37. Counts against `81a115d`: api compiled 1599/1595/4 unchanged and now stable across three runs, src 293/293 unchanged, suite 17 → 26 (+9, the wire-compatibility fixture from 2.6), contracts 236/236 unchanged, web 613 across 82 unchanged, `test:scripts` 236 → 248 (+12, the executed-schema gate's own cases). Every difference is a deliberate addition; nothing lost tests. Seven repository gates pass, including both contracts gates. `sandbox-environment` 2/2 and the six `test:sandbox` packages have zero failures. Two tests were edited and both were asserting the defect rather than the behaviour: `mcp-server-section.test.ts` served a bare array the api has never sent, and `auth.guard.spec.ts` used `id: 'tok-1'` where Prisma generates a uuid. A third, this change's own gate self-test, asserted that every test-only schema stays on the failure list — which stops being true the moment one is legitimately excepted, so the assertion is now test-only ⊆ (unexecuted ∪ excepted).
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes"]
  - surfaces: ["developer-workflow"]
  - verify: "workflow-gates"
- [x] 4.4 State what this change did NOT fix, by name: the inline-literal restatements still carried as exceptions, and anything 3.2 adjudicated as an exception rather than a call site. A change that reports a clean sweep its method cannot support is the thing both gates exist to prevent. **Still carried as exceptions on the IMPORT gate (14):** the inline-literal class — `OPERATOR_AUTH_SCHEME` ('Bearer' at ~30 sites), `AUTH_TOKEN_ENV_VAR`, `WS_AUTH_QUERY_PARAM`, `AuthTokenConfig`, `ReservedCredentialPrefix`, `IdentityProvider*`, `TaskFailureAction*` — plus the six `notifications.ts` exports, the three path-param shapes, `TASK_PROVISIONING_PROGRESS_CAPABILITY`, `PublicRestErrorProjectorKind` and `TaskProvisioningDiagnosticExpectation*`. **Still carried as exceptions on the EXECUTION gate (18)**, the ones that matter most being the two declarations the wire CONTRADICTS today and that this change deliberately did not wire: `AuditQuerySchema` declares `limit: z.number()` while HTTP query params arrive as strings, so `GET /audit/events` parses a file-local restatement with `z.coerce.number()` whose own comment admits the mirror; and `ForgeConnectionSchema`, violated on an input-dependent path. Each needs a contract fix BEFORE a parse — the AdminRevealResponse ordering — and each fix is a request surface this change did not scope. Also `Xterm550ResponseProfileSchema`, which declares that a drifted fingerprint "is not negotiable" and guards a negotiation that transmits only an id: closing it means changing what the attach frame carries. **What this change's own instrument still cannot see:** a value inlined as a literal (no declaration to match), and a parse reached through an indirection nobody declared. The second is now a known class rather than a surprise — three instances of it were found DURING this change and fixed, and the `INDIRECTION_POINTS` list is the record of the ones that remain by design.
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes"]
  - surfaces: ["developer-workflow"]
  - verify: "workflow-gates"

## 5. Track: verify-reopened (depends: none)

- [x] 5.1 Produce the dynamic evidence the change's own `surface-impact.json` demands. Verification reported all four mandatory lanes — `registry`, `restMetadata`, `mcpSdkMetadata`, `behavior` — as not passing, so every requirement rested on a static read plus hand-run scripts. **Three of the four now have real runs; the fourth is attributed, not claimed.** `registry`: `run-verifier contracts-registry` → 236/236. `restMetadata` + `mcpSdkMetadata`: `pnpm verify:public-surface` — the required `public-surface-parity` context — → 14/14 tasks, 82 pass / 1 skip / 0 fail, plus `run-verifier workflow-gates` → 83 tests, 82 pass / 0 fail. `behavior`: `tightened-request-surfaces.test.mjs` 9/9 and `drifted-contract-wire.test.mjs` 5/5, the latter driving the REAL compiled `SmtpController` and `RuntimesService`. **What is still NOT proven: a booted process.** `scripts/boot-smoke.sh` probes `POST /auth/admin/reveal` pre-auth — precisely the endpoint 2.1/2.2 changed — and it does not reach that probe on this machine: the app exits during bootstrap at `tasks.service.ts:276`, "startup provider sandbox inventory is indeterminate", because no sandbox provider is running locally. **Attributed rather than assumed**: stashing this change and rebuilding at `81a115d` fails at the same line with the same error, so it is the machine, not the change. `apps/api/CLAUDE.md` says not to treat a local boot-smoke failure as a regression without attributing it; this is that attribution. CI boots with a provider and will exercise the endpoint — that run is the remaining evidence, and it is a CI observation, not a task anyone can close at a keyboard.
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes", "monorepo-foundation/a-shared-type-shall-have-exactly-one-declaration", "monorepo-foundation/an-export-nothing-can-reach-shall-fail-the-build"]
  - surfaces: ["contracts", "public-v1", "mcp", "openapi", "playground"]
  - verify: "contracts-registry"
- [x] 5.2 Remove `zodToJsonSchema(X)` from `parsesSchema()`. `scripts/contracts-executed-schema-check.mjs:244` counts a JSON-Schema reflection call as proof a schema is executed against the wire, and the form has **zero** call sites repository-wide (the only two hits are the regex itself and its own test fixture at `scripts/contracts-executed-schema-check.test.mjs:32`). Reflection validates no wire bytes; counting it is exactly the reachable-but-unexecuted amnesty `specs/monorepo-foundation/spec.md:9-13` separates from execution, and it silently exempts the first OpenAPI-only reference someone adds. Delete the pattern and its test case, or — if it is wanted — make it a declared `INDIRECTION_POINTS` entry with a real file:line, which is what D2 requires of every non-textual execution path. **Deleted**, and the form moved to the gate's `naming a schema is not running it` case, where it belongs: reflection produces a document, not a verdict on any bytes.
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes"]
  - surfaces: ["contracts"]
  - verify: "contracts-registry"
- [x] 5.3 Remove the `parseZodValue(X)` literal-name pattern (`scripts/contracts-executed-schema-check.mjs:245`) for the same reason: zero real call sites. The actual `parseZodValue` call sites in `apps/api/src/public-surface/public-v1-operation.ts` pass a property path (`operation.input.params.parse`), never a schema name, which is precisely why D2 chose declared indirection over textual inference — and that indirection is already declared. Task 1.1 enumerates six recognised forms; these two make eight, and the two extra ones are blanket inference the design decision rejected. **Deleted; six recognised forms are now four**, and the gate still reports the same 374 executed / 0 unexecuted, which is the check that removing them cost no real attribution.
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes"]
  - surfaces: ["contracts"]
  - verify: "contracts-registry"
- [x] 5.4 Delete the two empty probe files left in the production contracts source directory: `packages/contracts/src/health.ts` (0 bytes, untracked — the executed-schema scan's `readdirSync(CONTRACTS_SRC)` reads it as a real contracts module) and `packages/contracts/src/task.js.refute-test` (0 bytes, untracked). Neither is referenced by any task, any design decision, or any code path; both are leftover gate-probe debris from Track 0/1 verification. **Deleted.** They were untracked, so no commit ever carried them — but `readdirSync(CONTRACTS_SRC)` did.
  - requirements: ["monorepo-foundation/an-export-nothing-can-reach-shall-fail-the-build"]
  - surfaces: ["contracts"]
  - verify: "contracts-registry"
- [x] 5.5 Correct `surface-impact.json` before archive, then re-run the sidecar check. Four surfaces are declared `unchanged` that the code evidence contradicts — `publicV1`, `mcp`, `openapi`, `apiPlayground` — see design.md Open Questions Q1-Q4. Archive cannot accept a sidecar claim the diff falsifies; either the declarations move to `changed` with an honest scope, or each `unchanged` reason is rewritten so it survives the evidence rather than restating the intent. **All four stay `unchanged` and all four reasons are rewritten**, answered in design.md Q1-Q4 with evidence: no file under `apps/api/src/v1/`, `apps/api/src/mcp/`, `apps/api/src/openapi/` or `apps/web/src/components/api/` is modified; `grep AdminReveal|McpToken` against the OpenAPI registry returns ZERO hits, so neither the new contract arm nor any added parse is projected into a published document; and `pnpm verify:public-surface` — the required `public-surface-parity` context — passes 14/14. Two things the first draft failed to say are now said: `/mcp-tokens` gains three outbound parses, so a drifting row now fails the request where it used to serve (declared under `internalOnly`), and the execution gate's indirection entry pins `/v1`'s parse site by file and line, a new coupling to the public surface with no byte on it moving.
  - requirements: ["monorepo-foundation/a-rule-the-contracts-package-states-shall-be-executed-against-the-wire-it-describes", "monorepo-foundation/a-shared-type-shall-have-exactly-one-declaration", "monorepo-foundation/an-export-nothing-can-reach-shall-fail-the-build"]
  - surfaces: ["public-v1", "mcp", "openapi", "playground"]
  - verify: "contracts-registry"
