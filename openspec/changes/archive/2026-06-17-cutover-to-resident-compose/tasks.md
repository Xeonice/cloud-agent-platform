<!-- Track-annotated tasks. Numbered groups are parallel Tracks; tasks within a track run serially. -->

## 1. Track: prod-observability (depends: none)

- [x] 1.1 In `docker-compose.prod.yml`, add the `loki` service: `image: grafana/loki:3.1.1`, `profiles: ["observability"]`, `command: ["-config.file=/etc/loki/config.yaml"]`, `volumes: [loki-data:/loki]`, `networks: [default]`, `mem_limit: 256m`, `restart: unless-stopped`, `logging` anchor, NO published port; attach inline config `loki-config` at `target: /etc/loki/config.yaml`.
- [x] 1.2 Add the `grafana-alloy` service: `image: grafana/alloy:v1.3.1`, `profiles: ["observability"]`, `depends_on: [loki]`, `command: [run, /etc/alloy/config.alloy, --storage.path=/var/lib/alloy/data]`, `volumes: [/var/lib/docker/containers:/var/lib/docker/containers:ro, alloy-data:/var/lib/alloy/data]`, `networks: [default]`, `mem_limit: 128m`, `restart: unless-stopped`, logging anchor; attach inline config `alloy-config` at `target: /etc/alloy/config.alloy`.
- [x] 1.3 Add the `grafana` service: `image: grafana/grafana:11.2.0`, `profiles: ["grafana"]`, `depends_on: [loki]`, `ports: ["127.0.0.1:3001:3000"]` (loopback only), `networks: [default]`, `mem_limit: 256m`, `restart: unless-stopped`, logging anchor, env `GRAFANA_PG_USER`/`GRAFANA_PG_PASSWORD`/`GF_AUTH_ANONYMOUS_ENABLED=false`/`GF_USERS_ALLOW_SIGN_UP=false`/`GF_SERVER_ROOT_URL=${GRAFANA_ROOT_URL:-}`/`GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-}`, `volumes: [grafana-data:/var/lib/grafana]`; attach 3 inline configs in long form at `/etc/grafana/provisioning/datasources/datasources.yaml`, `/etc/grafana/provisioning/dashboards/dashboards.yaml`, `/etc/grafana/dashboards/cap-observability.json`.
- [x] 1.4 Add the top-level `configs:` block with 5 entries (`loki-config`, `alloy-config`, `grafana-datasources`, `grafana-dashprovider`, `grafana-dashboard`), each `content: |` carrying the canonical file body; in `grafana-datasources` escape Grafana's runtime tokens as `$${GRAFANA_PG_USER}` / `$${GRAFANA_PG_PASSWORD}` (loki/alloy bodies verbatim, no `$`). Add `loki-data`, `alloy-data`, `grafana-data` to the top-level `volumes:` block. — Done via the generator (gen-prod-observability-configs.mjs --write); the dashboard JSON's `$taskId` is also escaped (universal `$`→`$$`).
- [x] 1.5 Ensure the `logging` YAML anchor and the `default` network are present/declared so `docker compose -f docker-compose.prod.yml config` parses; update the file's header comment (observability is now opt-in inline, not excluded; note Compose ≥ v2.23.1 floor).

## 2. Track: release-generator (depends: prod-observability)

- [x] 2.1 Add a small generator (script invoked by CI) that reads `deploy/observability/{loki-config.yaml, alloy-config.alloy, grafana/provisioning/datasources/datasources.yaml, grafana/provisioning/dashboards/dashboards.yaml, grafana/dashboards/cap-observability.json}` and renders the inline `configs.content:` blocks, applying `$ → $$` escaping. — `deploy/observability/gen-prod-observability-configs.mjs` (`--write`/`--check`); escapes ALL `$` (covers the dashboard `$taskId` too, not only the grafana-datasources tokens).
- [x] 2.2 Add a CI step (in `.github/workflows/release.yml`) that runs the generator in VALIDATE mode and FAILS the release if the committed `docker-compose.prod.yml` inline blocks do not match the canonical source or are missing the `$$` escape. — Added `verify-run-package` job; `build-push` + `attach-run-assets` both `needs:` it (no new push trigger — release.yml stays inert-on-push). actionlint clean.
- [x] 2.3 Document (generator README/comment) that `deploy/observability/*` is the single editable source and prod.yml inline blocks are generated — no hand-editing the YAML-in-YAML. — Documented in the script header + a new section in `deploy/observability/README.md`.

## 3. Track: docs-and-env (depends: prod-observability)

- [x] 3.1 `docker-compose.prod.env.example`: add a commented observability section — `COMPOSE_PROFILES=observability,grafana`, `GRAFANA_PG_USER`/`GRAFANA_PG_PASSWORD`/`GRAFANA_ADMIN_PASSWORD`/`GRAFANA_ROOT_URL` (all optional, only needed when the `grafana` profile is on) — with a one-line pointer to the grafana-ro-role.sql step.
- [x] 3.2 `deploy/DEPLOY.md`: add the Dokploy → resident-compose cutover runbook (§11.4): pre-pull, `pg_dump` backup, Stop/disable the Dokploy cap app, `docker compose -p cloud-agent-platform -f docker-compose.prod.yml up -d`, verify, rollback. Calls out the 3 must-dos (`-p`, pull-before-up, reuse `files/api.env` verbatim).
- [x] 3.3 `deploy/DEPLOY.md` (§11.5) + `docs/self-hosting.md`: add the observability opt-in section — `COMPOSE_PROFILES` self-select, the `grafana-ro-role.sql` + `GRAFANA_PG_*` manual step, Grafana loopback/tunnel exposure, the Compose ≥ v2.23.1 floor, and Alloy's host assumptions. Also fixed the now-stale "observability not in the run package" lines in §11.3 + self-hosting.md.

## 4. Track: verify-local (depends: release-generator, docs-and-env)

- [x] 4.1 `docker compose -f docker-compose.prod.yml config` with NO profile renders ONLY the core services (no loki/alloy/grafana) and materializes none of the observability configs. — Verified: services = api/aio-sandbox-image/postgres; materialized config keys = 0.
- [x] 4.2 With `COMPOSE_PROFILES=observability,grafana`, `config` renders all three observability services with their inline configs at the correct file `target:`s; assert the `$$` escape survived. — Verified: +loki/grafana-alloy/grafana render.
- [x] 4.3 Bring the observability profile up locally and confirm loki `/ready`, grafana-alloy tails `/var/lib/docker/containers`, grafana provisions the Loki datasource + dashboard. — Live test on Docker 29.5.3 / Compose v5.1.4 (throwaway project, torn down): grafana `/api/health`=200; `/api/datasources` shows Loki + Postgres-Audit with `user=sentinel_ro` (proves `$$GRAFANA_PG_USER` un-doubled in-container + Grafana runtime-expanded it); dashboard `cap-observability` loaded; provisioning log clean. Container `datasources.yaml` shows single-`$` `${GRAFANA_PG_USER}`; dashboard shows single-`$` `$taskId`.
- [x] 4.4 Run the release generator in validate mode against the committed `docker-compose.prod.yml` and confirm it passes (no drift, escape present). — `--check` → "in sync and `$`-escaped — OK".

## 5. Track: cutover-execution (depends: verify-local) — OPERATOR-GATED

- [ ] 5.1 (Maintainer-run, requires explicit go) Execute the cutover runbook on the prod host per §11.4; the assistant does not perform the irreversible infra steps autonomously.
- [ ] 5.2 (Maintainer-run) Post-cutover live verification: `/version` reports the real gitSha/buildTime, a task runs end-to-end on a freshly-pulled ghcr AIO sandbox, DB rows intact, Cloudflare edge serves; optionally enable `COMPOSE_PROFILES=observability,grafana` and confirm Grafana Loki panels populate.
