// Fixture — the canonical credential decryption helper; unique in this tree, so
// only the origin seam is expected to be reported.
export function decryptStored(value: string): string {
  return value;
}
