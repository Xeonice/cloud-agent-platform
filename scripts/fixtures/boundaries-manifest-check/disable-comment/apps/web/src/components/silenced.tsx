/* eslint-disable no-restricted-imports -- silences the lint layer, not this gate */
// eslint-disable-next-line @typescript-eslint/no-restricted-imports
import { createTask } from "@cap-console/api";

export const create = createTask;
