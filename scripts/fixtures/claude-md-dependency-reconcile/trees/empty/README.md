Fixture tree with no governed CLAUDE.md anywhere — the "the paths moved" case.
The gate must report the empty governed set rather than pass because it found
nothing to disagree with. This file exists only so the directory is tracked.
